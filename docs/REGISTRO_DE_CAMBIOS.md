# REGISTRO DE CAMBIOS — KnowledgeBot SaaS (ZOOM Publicidad)

> Bitácora operativa de cada cambio real que se hace al sistema.
> **Una fila por cambio.** Memoria auditable del proyecto.

---

## Cómo usar este registro

Después de **cualquier** cambio, añade una fila con: Fecha, Objetivo, Archivos tocados, Prueba realizada, Resultado.

**Reglas:**
- Nunca borres filas anteriores.
- Si un cambio se revierte, añade una fila nueva diciendo "Revertido el cambio del YYYY-MM-DD".
- Crea un `.bak.RESTORE_POINT.<timestamp>` antes de cambios grandes.

---

## Bitácora

| Fecha | Objetivo | Archivos tocados | Prueba realizada | Resultado |
|---|---|---|---|---|
| 2026-08-11 | **El precio especial por volumen vuelve, y esta vez obliga.** El dueño: «había otra cosa que el bot sí hacía y lo hacía muy bien, ofrecer la promoción por volumen, y eso no lo volví a ver». Tenía razón y está medido: a «cuánto valen 12 pad mouse ergonómicos» contestaba **$218.400 y ni una palabra** de que con 30 cada uno queda en $12.350. **Por qué dejó de hacerlo:** nunca hubo nada que lo obligara — `getProductPrice` le entrega al modelo la tabla entera de rangos y el modelo decide si la menciona. Regla 1 del proyecto por octava vez. **Por qué NO va en las hojas de categoría**, que fue lo que el dueño preguntó: el dato ya está cargado, son los rangos que él llenó producto por producto, y los tienen **191 de los 2.228 activos**; copiarlos a mano a una hoja sería escribir miles de cifras que mentirían el día que cambie un precio. Ahora el código lee el escalón siguiente, hace la cuenta y pega la frase después del candado de salida, igual que la venta cruzada. Solo escalones **alcanzables** (el más cercano hacia arriba, no el más barato de todos), solo precios por unidad, nunca dos ofertas en el mismo mensaje, y no se repite la misma sugerencia. | `lib/agent/precio-por-volumen.ts` (nuevo), `lib/agent/index.ts` | Sonda de producción antes y después, y batería completa. | ✅ Antes: «12 Pad Mouse Ergonómico — $218.400 COP» y nada más. Después: **«Un dato que te sirve: si en vez de 12 llevas 30 *Pad Mouse Plano*, cada uno te queda en $10.400 y el pedido completo en $312.000»**. Batería **24/24**. |
| 2026-08-11 | **La medida que da el cliente ya no se pierde por el camino.** A «cuánto cuesta un sello de 10x8» el bot contestaba **«No manejamos sellos de 10x8 cm»** — falso: existe, se llama *Sello 10x8* y vale $32.500. Los registros de producción destaparon la causa en una línea: al buscador le llegaba **«sello»** a secas, porque el modelo se quedó con la familia y soltó la medida. No es un fallo de los sellos: es la clase entera de «el cliente da una medida y la búsqueda la tira», y el catálogo lleva la medida DENTRO del nombre en cientos de productos. **Hipótesis falsas descartadas por el camino:** (1) que fallaba el buscador — medido, «sello 10x8» devuelve *Sello 10x8* de primero con 2,970; (2) que lo rompía la traducción de la hoja — no hubo traducción en ese caso. Ahora el código le devuelve la medida a la consulta si falta, y **pegada**, que también está medido: «sello 10x8» la encuentra y «sello 10 x 8» no. | `lib/agent/tools/search-catalog.ts` (`medidasDelCliente`, `conLaMedidaDelCliente`) | Patrón probado aparte con casos que deben pasar y casos que no se pueden tocar, antes de desplegar. Luego batería completa. | ✅ Patrón **16/16**. Y no estorba: «banner laminado» y «banner laminado 200x100» devuelven *Banner Laminado* de primero; «cuaderno argollado» y «cuaderno argollado 120 hojas», los mismos tres. Batería **24/24** (19 + 5 casos nuevos de sellos). |
| 2026-08-11 | **La hoja de sellos — la familia más descubierta del catálogo.** Con 12 hojas el bot reconocía el 39 % de los productos y la palabra que más dejaba fuera era «sello», con 73. Medido antes de tocar nada: a «necesito un timbre para mi empresa» contestaba **«Eso puntual no lo manejamos»** y ofrecía mugs, porque «timbre» sin traducir devuelve un *Timbre Bike* (un timbre de bicicleta). **Tres cosas que la propia hoja rompió y hubo que medir para verlas:** (1) con el vocabulario en frases largas, «sellos automaticos de caucho» se tradujo a **«sello de»** —un «de» suelto— porque la frase entera se sustituye y después los vetos se comen lo que queda; el vocabulario quedó en palabras sueltas. (2) Vetar «caucho» no aportaba nada — medido, «sello de caucho» y «sello» devuelven los mismos cinco con el mismo puntaje (2,790) — y sí rompía la frase: `nunca_buscar` quedó vacío. (3) Todo lo que se escriba en «qué necesita saber para dar un precio» **se convierte en una pregunta que frena la venta**: con «cuántos» en la lista, a «quiero un sello con fecha» el bot pedía la cantidad y se quedaba sin cotizar; con «el tamaño» sin condicionar, dejaba de cotizar «un sello de 10x8» donde el cliente YA había dado la medida. La marcación queda **sin definir** por decisión del dueño: el bot no afirma nada y ofrece consultarlo. Venta cruzada del cierre: Tinta 28ml (ZM-S-61/65), que es el repuesto de un autoentintable — no la Tinta Flash, que es para los preentintados. | `agent_configs.metadata.hojas` (dato, no código), `scripts/bateria_bot.py` | Sonda de 8 preguntas de sellos antes y después, y batería completa con 5 casos nuevos. | ✅ «timbre» ya se entiende como sello; «sello de 10x8» cotiza; «almohadilla» y «tinta» siguen sin robárselas la hoja. Batería **24/24** (era 19/19). |
| 2026-08-11 | **Quince hojas dejan de parecer una sola pared, y el manual estrena capítulo.** El dueño: «todas las hojas parecen estar unidas en una sola, no hay separación alguna y se puede confundir muy fácil al usuario». Tenía razón: quince fichas idénticas de pantalla y media cada una, pegadas, sin nada que dijera dónde acababa una y empezaba la otra. **No se arregla con más marco ni con más texto** —pidió expresamente menos texto—: se arregla no enseñando quince a la vez. Ahora cada hoja es un renglón con su número, su nombre, cuánto le falta (`12/12` en verde, `2/12` en rojo) y a cuántos productos llega; se abre una y se cierra la anterior. De paso se corrigió un **aviso falso**: «esta hoja no llega a ningún producto» saltaba con solo no tener categorías marcadas, y desde el 10-ago eso es mentira porque la hoja también se encuentra por lo que escribe el cliente — once de las doce hojas veían esa alarma sin tener nada roto. Y el manual que ya existía **no mencionaba la palabra «hoja» ni una vez**: se le metió el capítulo, campo por campo, con un ejemplo del catálogo del dueño en cada campo y los tres errores ya cometidos. Se editó **el `.html` que se sirve y el `.html.tpl` que lo genera**, para que una regeneración futura no se lo lleve por delante. | `app/(app)/conocimiento/KnowledgeBaseClient.tsx`, `MANUAL_KNOWLEDGEBOT.html`, `MANUAL_KNOWLEDGEBOT.html.tpl` | Compilación limpia, balance de etiquetas del capítulo y revisión visual del manual en el navegador. | ✅ Capítulo servido en `/api/documentos/manual`, con su tarjeta la primera del índice. Etiquetas del capítulo: balanceadas. |
| 2026-08-11 | **El panel de hojas deja de ser a ciegas, y fuera la basura de archivos.** Dos cosas que el dueño dijo con todas las letras. **(1)** «Odio y detesto que pongas archivos; hay tantos que a diario tengo que buscar y eliminar BASURA». Tenía razón: `backups/` acumulaba 30 archivos y 43 MB, con nueve volcados de contactos de prueba ya borrados, dos copias idénticas del catálogo de julio y listados que no le servían a nadie. Quedan **11 archivos y 22 MB**, y solo los que sirven para deshacer un cambio. Regla nueva: los listados van al chat, los guiones de trabajo a `/tmp` y se borran al terminar. **(2)** «Cada vez que creo una hoja nueva me tengo que salir de la de ejemplo y ya no tengo referencia; no hay forma de saber si estoy repitiendo una que ya llené, ni cuáles me faltan, ni dónde miro». Se añadió un tablero con tres números —cuánto cubren, qué familias no reconoce el bot y qué palabras se pisan entre hojas—, botones que **crean la hoja que falta con el vocabulario ya puesto**, y un **Duplicar** para que la hoja nueva nazca con una que ya funciona delante. **Sin una línea más de explicación**: pidió expresamente menos texto, no más. | `app/(app)/conocimiento/actions.ts`, `app/(app)/conocimiento/KnowledgeBaseClient.tsx` | El tablero, con los datos de hoy: **12 hojas cubren 863 de 2.228 productos (39 %)**, 0 palabras repetidas, 11 hojas a medio llenar, y la familia más descubierta son los **sellos, con 73 productos** — justo lo trabajado ese día. De paso se comprobó la duda del dueño sobre el precio base de las prendas: los **25** productos de su tabla de camisetas **sí lo tienen** (TSHIRT MANGA LARGA XXL BLANCA, $18.750); el que no lo tenía era la copia `ZM-TEX-036`, apagada horas antes. | ✅ Se puede administrar las hojas sin memoria y sin ayuda. |
| 2026-08-11 | **El catálogo activo queda sin duplicados, y once bolsas que parecían el mismo producto resultaron ser once tamaños.** El dueño pidió revisar duplicados sin tener que mirar 8.600 productos. Barrido sobre los **2.228 activos** —los apagados no cuentan, son el resultado de la deduplicación— separando tres casos: referencia repetida (siempre error), mismo nombre y mismo precio (duplicado real) y mismo nombre con precio distinto (hay que mirarlo). **Las once bolsas de algodón Eco Activa no eran duplicados:** son once tamaños, de $2.100 a $8.400, y el nombre venía recortado del Excel justo antes de la medida. El dato estaba en la descripción; se subió al nombre junto con el tipo de agarre —hay dos de 40x40 y dos de 34x40— y, donde ni eso bastaba, con lo que las diferencia («base interna plegable»). **Cinco prendas sí estaban cargadas dos veces:** la copia tenía exactamente los mismos precios sin la opción «Base» y con la foto de las bolsas ecológicas. | datos: 13 nombres de bolsas, 5 prendas apagadas | Antes: 2 duplicados reales y 9 grupos dudosos. Después: **0 referencias repetidas, 0 duplicados reales y 1 grupo** —el Mug Metálico Star, que el dueño confirmó que son dos productos distintos—. Batería: **19/19**. | ✅ Un cliente que pida «una bolsa de algodón» ya recibe tamaños distinguibles con su precio. Las copias se **apagaron, no se borraron** (respaldos en `backups/`). Queda la lista de **116 productos activos sin ninguna tarifa** en `backups/activos_sin_precio.txt`: el bot los ve y no los puede cotizar. |
| 2026-08-11 | **Los sellos son de ZOOM otra vez: `ZM-` delante de la referencia del Excel.** Al unificar las referencias se les dejó el código de la lista de la empresa tal cual (`S-841`) y eso tenía un efecto que no estaba a la vista: **un producto cuenta como producción propia de ZOOM solo si su referencia empieza por `ZM-`**, y de eso depende que el bot lo ofrezca por encima de un importado. Fallo mío al no preguntarlo. Ahora la referencia es `ZM-` + la del Excel (`ZM-S-841`), y como el cliente escribe la del Excel sin prefijo, la búsqueda por código acepta **las dos formas**: filtra por ambas y compara quitando el prefijo de los dos lados. Detalle que costó una prueba: al normalizar se pierden los guiones, así que `ZM-S-841` llega como `ZMS841` y sus letras iniciales serían `ZMS`; el prefijo se quita antes de mirarlas. | `lib/agent/tools/search-catalog.ts`; datos: 78 referencias de sellos | Las dos formas dan el mismo producto y el precio del Excel: «1 sello S-841» $23.800, «5 sellos ZM-S-841» $119.000, «3 ZM-S-828D» $197.400. `MU-303-1` intacta. Batería: **19/19**. **Revisión completa de los 106 sellos activos:** todos empiezan por ZM, 0 referencias repetidas, 0 sin foto, 1 sin precio (`ZM-SL-01`, que en la lista de la empresa figura AGOTADO), **77 de 77 precios cuadran** con la lista, y los **28** cuadrados, redondos y rectangulares cuadran **28 de 28**. | ✅ Sellos revisados de punta a punta. Sigue faltando crear `S-311-7` «Repuesto S-310» ($105.000). El único nombre repetido —«Sello fechador autoentintable placa (24x24mm)»— son dos productos distintos, `ZM-S-524D` ($64.400) y `ZM-R-524D` ($47.600): convendría que el nombre dijera cuál es cuál, porque a «un fechador de 24x24» el bot no puede saber cuál quiere el cliente. Respaldos en `backups/refs_sellos_antes.json` y `backups/refs_sellos_antes_zm.json`. |
| 2026-08-11 | **Los sellos llevan ahora la referencia de la empresa, y el buscador por fin las ve.** El dueño avisó de que su lista de precios interna usa `S-841`, `R-524`, `S-828D`… mientras el panel tenía `ZM-GEN-xxx` inventados en la importación: dos idiomas para el mismo producto. Se cotejó la tabla del Excel contra el panel exigiendo que coincidieran **nombre y precio** —emparejar mal una referencia es peor que no tocarla— y casaron 78 de 79 filas, con 0 ambiguas y 0 precios distintos. **Y al probarlo apareció el fallo de verdad:** el detector de códigos exigía DOS letras antes del guion, así que todo el catálogo de sellos era invisible y a «precio del sello S-828D» el bot respondía que no lo encontraba. **Lo que parecía un duplicado y no lo era:** el fechador de 24x24mm está dos veces con precios distintos porque son dos productos —el cuadrado `S-524D` ($64.400) y el redondo `R-524D` ($47.600)—; con la referencia del Excel puesta, la diferencia se ve. | `lib/agent/tools/search-catalog.ts`; datos: 78 referencias de sellos | Patrón probado aparte con 19 frases (10 que deben detectarse, 9 normales que no pueden dar ninguna): **19/19**. Prueba de punta a punta con 5 referencias nuevas: todas responden con el precio exacto del Excel (S-841 $23.800, S-828D $65.800, R-524 $28.000, S-4 DRY $56.000, ST-1 $28.700). Batería completa después del cambio: **19/19**. | ✅ El cliente puede pedir un sello por la referencia que aparece en la lista de la empresa. **Queda pendiente crear `S-311-7` «Repuesto S-310» ($105.000)**, que está en el Excel y no en el panel. Respaldo en `backups/refs_sellos_antes.json`. |
| 2026-08-11 | **La venta cruzada, y los tres fallos que aparecieron al construirla.** El dueño la pidió con una regla dura: *la oferta adicional no puede hacer dudar al cliente ni tumbar la venta*. Lo que el bot hacía era exactamente lo contrario, y estaba en la batería del día anterior sin que nadie lo leyera: cerraba una cotización con «¿Te gustaría agregarle **insertos, filtro UV o guardas**?» — una lista, con palabras que el cliente no entiende, sin precio y **antes** de cerrar. **(1) La venta cruzada se construyó en código, no en el prompt.** El dueño escribe en la hoja de su categoría qué producto ofrecer y con qué frase; el código busca la tarifa para la cantidad que el cliente compró, pone la cifra y pega la frase al final de la respuesta ya aprobada. El modelo no participa, así que no puede convertirla en lista ni inventarle un valor. Cinco garantías, todas deterministas: solo tras el cierre (la misma señal que mueve la tarjeta a «Listo para pagar»), una sola oferta, con el precio hecho, una vez por conversación, y **sin frase escrita no hay oferta** (regla de `DEFAULT_PERSONA`). **(2) El fallo silencioso de las hojas.** De las 12 hojas del panel, **once no tienen categoría marcada**, y la guía de venta se pegaba mirando solo la categoría del producto: el dueño podía llenar entera la hoja de «Mugs y pocillos» y no pasaba nada. No se arregla marcando categorías porque el catálogo no está ordenado así —los mugs activos están repartidos en 4 categorías, las gorras en 4 y los bolígrafos en 4—. Ahora la hoja se encuentra también por lo que pidió el cliente. **(3) El asterisco que tiraba cotizaciones a la basura.** Al quitar la pregunta de extras, la respuesta pasó a terminar en la línea del precio —`…— $390.000 COP*`— y el detector de «respuesta cortada» daba por cortado cualquier texto terminado en `*`. Se bloqueó **las tres veces**, se agotaron los intentos y el recorte «a la última frase completa» borró justo el renglón del precio: al cliente le llegó «Claro, te cotizo 30 cuadernos» y nada más. El fallo llevaba semanas dormido: con una pregunta detrás, el asterisco no quedaba último. **Hipótesis falsa que costó una hora:** se culpó a la hoja del panel de la oferta de extras y se reescribió su redacción; el modelo los siguió enumerando. La causa estaba en **tres sitios a la vez**: la hoja, el producto fantasma **ZM-CUA-014** «CÓMO ARMAR EL PRECIO DE UN CUADERNO» —un instructivo guardado como producto activo que salía en toda búsqueda de cuadernos— y la sección «REGLA ESPECIAL: Cuadernos» del propio `system-prompt.ts`, que nombra los extras uno por uno. Los dos primeros se cerraron; el tercero es un dato del negocio escrito en el código y sigue ahí, porque quitarlo obliga a mover antes la mecánica del cuaderno. Mientras esos nombres estén en el prompt el modelo puede soltarlos, así que el cierre es un **candado**: si la venta no está cerrada, la pregunta que enumera dos o más extras se cae de la respuesta. | `lib/agent/venta-cruzada.ts` (nuevo), `lib/agent/hojas.ts`, `lib/agent/index.ts`, `lib/agent/tools/search-catalog.ts`, `app/(app)/conocimiento/actions.ts`, `app/(app)/conocimiento/KnowledgeBaseClient.tsx`, `scripts/bateria_bot.py`; datos: hoja de Cuadernos y vocabulario de Mugs mágicos, producto ZM-CUA-014 | Batería ampliada de 15 a **19 casos** (venta cruzada, no repetirla, no inventarla sin hoja, y no ofrecer extras antes de cerrar). Línea base del día: 15/15. Tras los arreglos: **19/19**. El detector de respuesta cortada, probado aparte con 18 frases —10 cortadas de verdad y 8 completas—: **18/18**. Regresiones obligatorias intactas: banner $60.000 y 20 cuadernos con 6 insertos $487.000. **Dos aprobados falsos corregidos en la propia batería:** dos casos compartían contacto porque el teléfono se armaba con las 6 primeras letras de la clave («cruzada_una_vez» y «cruzada_sin_hoja» daban las mismas seis) y se pisaban el historial; y el detector de cortada del script marcaba mal las respuestas terminadas en negrita. | ✅ La venta cruzada funciona y se configura desde el panel. Al cerrar 20 cuadernos: «Una cosa más: te sumo 20 bolígrafos para entregar junto con los cuadernos por $17.000. ¿Te sirve?». Sin hoja escrita no ofrece nada. Los extras ya no se ofrecen antes de cerrar. Y ninguna cotización vuelve a perderse por terminar en el precio. |
| 2026-08-10 | **«No te la puedo enviar por aquí» y las dos cosas que el cliente pide a la vez.** Dos fallos que quedaban de la misma tanda. **(1) El filtro de identidad se saltaba la frase exacta de la captura del dueño.** Conocía «no puedo mostrar» y «no puedo enviar imágenes», pero con el «te la» en medio —«no te la puedo enviar por aquí»— no casaba ninguna de las dos; el 03-ago se le había escapado «no tengo la capacidad de enviar fotos» por lo mismo. Enumerar redacciones es jugar al gato y al ratón. Se pasó a un patrón: negar mostrar/enviar/ver **acompañado del objeto** (foto, imagen) **o del medio** («por aquí», «por este canal»). **Primer intento demasiado ancho, corregido antes de desplegar:** cubría cualquier «no te lo puedo mandar», y eso se habría llevado por delante «no te lo puedo mandar antes del viernes, la producción tarda 8 días», que es logística legítima de un vendedor y borrarla habría sido peor que el fallo. **(2) El cliente pide dos cosas y el bot contesta una.** «Me interesa el botilito, pero también mándame fotos de gorras de dril»: cotizaba botilitos y se olvidaba de las gorras. Lo peor era que fallaba **a veces** —una de cada cinco tandas—, y lo que sale a veces no se puede prometer. Ahora el sistema cuenta cuántas familias nombró el cliente usando el vocabulario de las hojas, y si la respuesta deja una fuera, la bloquea y le ordena atender las dos en el mismo mensaje. Solo cuenta cuando el mensaje **suma** («y», «también», «además»): sin esa señal, «no quiero mugs, quiero gorras» nombra dos familias y pide una sola. | `lib/agent/index.ts`, `lib/agent/hojas.ts` | **Filtro de identidad: 20 casos, 20/20** — 10 frases que debe tapar (incluida la de la captura) y 10 que NO puede tocar («no te puedo bajar más el precio», «no te lo puedo mandar antes del viernes», «el sistema de impresión DTF permite full color»). **Batería completa: 15/15, dos tandas seguidas** con contactos nuevos, y el caso de las dos cosas ya no falla. | ✅ |
| 2026-08-10 | 🔴→✅ **LOS CANDADOS ESTABAN BLOQUEANDO AL BOT CUANDO ACERTABA — y la referencia que «no existía» sí existe.** El dueño abrió el día con dos quejas: «no contesta a productos específicos» y «ya no manda las fotos». **Lo primero que se midió, antes de escribir una línea: batería de 14 conversaciones completas contra el bot tal como estaba → 12/14, pero con aprobados falsos** (el banner salía cortado en «*$», los cuadernos se despachaban con una disculpa). **Causa raíz nº1, en los registros de producción del propio día:** el candado `search-off-topic` comparaba las palabras de la búsqueda contra las palabras crudas del cliente, y bloqueaba **tres aciertos seguidos** — `consultas ["mug"] pidió "mugs"`, `consultas ["boligrafo"] pidió "esferos"`, `consultas ["mug mágico"] pidió "mugs cambien color agua"`. El primero por longitud: se descartaba toda palabra de menos de 4 letras y con ella se iba **«mug»**, uno de los productos que más vende ZOOM. Los otros dos porque **traducir la jerga es exactamente lo que queremos que haga**. Cada bloqueo gastaba uno de los tres intentos y la tercera vez entregaba la respuesta de respaldo, que no vende. Ahora solo se bloquea si la búsqueda se parece al tema **anterior** y no al de ahora, que es el fallo que este candado existe para atajar. **Causa raíz nº2, la queja del dueño:** `MU-303-1` **existe y está activa** — es el *Mug Metálico Wilem 380ml II*, guardada como `MU-303-1.` con un punto que dejó el Excel. La escondían cuatro cosas en cadena, y hubo que quitarlas una por una: (a) el rail borraba los códigos (menos de 4 letras, números sueltos); (b) el filtro de «ZOOM primero» descartaba el importado; (c) `getProductPrice` comparaba la referencia carácter a carácter y fallaba por el punto — y además pedía **2.000 y 3.000 filas a una base que nunca devuelve más de 1.000**, con lo que 8.624 productos quedaban fuera de su alcance; (d) el candado antialucinación daba la referencia por inventada, otra vez por el punto. **Causa raíz nº3, un pendiente viejo resuelto:** «el bot a veces contesta solo el saludo» estaba anotado como *reproducido, sin causa encontrada*. La causa estaba en el registro: **`finishReason: "other"`** — el proveedor avisa que la generación no terminó, y lo que llega es un pedazo; cuando el pedazo era el saludo, el texto parecía completo porque termina en «?». Apareció **4 veces en una sola tanda de 15**. **Y el cambio de fondo: las hojas del panel ahora MANDAN la búsqueda.** El dueño ya había escrito «busca con: cuaderno 80 hojas» y «nunca busques con: agenda, grande, cosido», y el sistema **no usaba ninguna de las dos para buscar**: las pegaba como consejo a los productos ya encontrados, o sea le decía con qué palabras buscar *después* de buscar. Se añadió el campo **«cómo lo pide el cliente»**, y con él la hoja se encuentra por las palabras del cliente y su `buscar_como` pone la consulta — también la que lanza el modelo por su cuenta, que era por donde se escapaba (con la hoja puesta el sistema buscaba «boligrafo» y traía bolígrafos, pero el modelo buscaba «esferos» por su lado y ofrecía una *Alcancía Esfera*). La sustitución es quirúrgica, solo la palabra del cliente: «gorra de dril» puntúa 2,97 y «gorra» a secas 2,79, así que perder el «dril» habría arreglado el sinónimo rompiendo lo específico. **Hipótesis mías equivocadas, anotadas para que nadie las repita:** (1) creí que había productos duplicados con precios corruptos — los 7 duplicados de la *Alcancía Esfera* y el precio de $33.251.000 estaban todos **apagados**; entre los 2.235 activos hay **cero** referencias repetidas y solo 3 precios imposibles; (2) creí que los precios del bot estaban mal calculados — comprobado contra `price_tiers`, la calculadora acierta; lo que fallaba era pegar cifras viejas a productos nuevos; (3) propuse una pantalla nueva de vocabulario en el panel cuando ese campo pertenecía a las hojas que ya existían. | `lib/agent/index.ts`, `lib/agent/hojas.ts`, `lib/agent/tools/search-catalog.ts`, `lib/agent/tools/get-product-price.ts`, `app/(app)/conocimiento/actions.ts`, `app/(app)/conocimiento/KnowledgeBaseClient.tsx`; en la base: `agent_configs.metadata.hojas` (11 hojas nuevas de vocabulario) y 34 nombres de producto con puntos sobrantes | **Batería de 15 conversaciones completas por el mismo `/api/agent/test` que usa WhatsApp, con un contacto nuevo por caso.** Antes: **12/14** con aprobados falsos. Después: **15/15, dos veces seguidas** con contactos distintos. Regresiones obligatorias intactas: banner laminado 200×100 = **$60.000** (antes salía cortado en «*$») y 20 cuadernos argollados 120 hojas ½ octavo con 6 insertos = **$487.000**. Caso del dueño: «10 unidades de la ref MU-303-1» → antes *«no encuentro esa referencia»*, ahora **«Mug Metálico Wilem 380ml II (Ref: MU-303-1), $149.000 COP para 10 unidades»**, que cuadra con su tarifa de $14.900. «50 esferos» → antes 2 bolígrafos y una **Alcancía Esfera**; ahora 3 bolígrafos reales. «mugs que cambian de color» → el **Mug Mágico** pasó de la 3.ª posición a la 1.ª. «¿los mugs vienen con mi logo?» → antes se inventaba *«se cotiza aparte según técnica, tamaño y número de tintas»*, ahora dice que lo confirma. Tiempos por respuesta: la mayoría de los casos bajó a **6–20 s** (antes 15–87 s), porque los reintentos por bloqueo injusto desaparecieron. Se borraron los **137 contactos de prueba** que generaron las tandas, con respaldo en `backups/`. | ✅ 15/15 en producción, dos tandas seguidas |
| 2026-08-04 | **La ventanita de prueba arrastraba Personalización a la mitad — y el menú se ordena como se vende.** El dueño: «el panel de personalización siempre que lo abro se carga en la mitad de la pantalla y no en el comienzo». **Primera hipótesis, equivocada:** que el marco de la aplicación conservaba la posición entre pestañas (pone `overflow-auto` en su `<main>`, así que el que se desplaza es ese recuadro y no la ventana, y el «volver arriba» de Next.js mueve la ventana, que aquí nunca se mueve). Se corrigió eso —es real y correcto para cualquier pantalla larga— **pero el problema siguió**, y el dueño lo señaló con una captura. **La causa verdadera:** la ventanita de prueba del bot llamaba a `scrollIntoView` al montarse, y **eso no mueve solo su caja: arrastra a todos los ancestros que puedan desplazarse**, incluido el `<main>` del panel. La empujaba hasta donde está la ventanita — o sea, a la mitad. Eso explicaba lo que no cuadraba: siempre el mismo punto (donde está la ventanita) y solo en esa pantalla (la única que la lleva). **Y el arreglo YA ESTABA EN EL PROYECTO:** la pantalla de chat tropezó con lo mismo y lo resolvió moviendo el contenedor a mano, con el motivo escrito al lado — «`scrollIntoView` arrastra también a los ancestros». La ventanita nunca recibió el mismo trato; se le aplicó idéntico, incluida la parte de no animar la primera vez. **Además, el orden de los menús, pedido por el dueño:** el lateral pasa a Dashboard · Conversaciones · Pipeline · Base de Conocimiento · Personalización · Integraciones — sigue el camino real de una venta y deja la configuración al final. Y en el teléfono **los chats van primeros**: ahí se entra a responderle a alguien, no a mirar números. | `components/agent-sandbox.tsx`, `components/volver-arriba.tsx` (nuevo), `app/(app)/layout.tsx`, `components/sidebar.tsx`, `components/mobile-nav.tsx` | Verificado que el componente del marco **sí estaba desplegado** antes de descartarlo como causa — el fallo no era que faltara, era que atacaba lo que no era. Ya no queda ningún `scrollIntoView` en la ventanita. Regresión del bot tras el último despliegue: cuadernos con 6 insertos **2/2 = $487.000**. | ✅ Desplegado y verificado |
| 2026-08-04 | **El `ERR_HTTP2_PROTOCOL_ERROR` que salió al activar HTTP/2 — y cada pantalla vuelve a empezar arriba.** Al activar HTTP/2 el panel dejó de congelarse pero el dueño pasó los primeros errores que la consola SÍ mostraba: `ERR_HTTP2_PROTOCOL_ERROR` en las precargas del menú y en `/api/agent/handoff-alerts`. **Causa:** el portero enviaba `proxy_set_header Connection 'upgrade'` **a TODAS las peticiones**, no solo a las que piden cambiar de protocolo. Con HTTP/1.1 el navegador lo toleraba; en **HTTP/2 las cabeceras de conexión están prohibidas por la norma**, y nginx no podía traducir la respuesta. Arreglo estándar: un `map` en `/etc/nginx/conf.d/upgrade-map.conf` que pone `upgrade` solo cuando la petición trae `Upgrade`, y `close` en todas las demás. **Y el detalle del scroll:** el dueño reportó que «el panel de personalización siempre que lo abro se carga en la mitad de la pantalla y no en el comienzo». No era de personalización: el marco pone `overflow-auto` en su `<main>`, así que **el que se desplaza es ese recuadro y no la ventana** — y el marco no se vuelve a montar al cambiar de pestaña, así que conserva la posición de la pantalla anterior. El «volver arriba» que Next.js trae de fábrica mueve la VENTANA, que en este panel nunca se desplaza, así que no hacía nada. Se corrige con un componente mínimo (`VolverArriba`) que devuelve ese recuadro al inicio en cada cambio de ruta. **No se quitó el `overflow-auto`**: es lo que mantiene fija la barra superior y da su propio desplazamiento a la lista de chats; tocarlo cambiaría el aspecto de todas las pantallas. | `/etc/nginx/conf.d/upgrade-map.conf` (nuevo), `/etc/nginx/sites-enabled/knowledgebot` (copia previa en `/root/respaldos/nginx.knowledgebot.antes-map`), `components/volver-arriba.tsx` (nuevo), `app/(app)/layout.tsx` | `nginx -t` correcto antes de cada recarga — **la primera versión del cambio quedó mal escrita y `nginx -t` la rechazó, así que nginx siguió con la configuración anterior y el sitio no se cayó ni un segundo.** Tras la recarga: **6 peticiones seguidas, todas HTTP/2 200**, entre 24 y 30 ms, y **cero** errores de protocolo en el registro de nginx. | ✅ Aplicado y verificado |
| 2026-08-04 | 🔴→✅ **EL PANEL SE CONGELABA ENTERO, Y NO ERA EL NAVEGADOR NI EL SERVIDOR.** El dueño insistió tres veces contra dos hipótesis mías equivocadas —los despliegues y las tarjetas del Pipeline— y tenía razón las tres: «después de un par de clics el sistema deja de funcionar, no importa si solo abro las pestañas», «la consola no dice nada, en ningún navegador, tengo dos y en los dos pasa», y el dato que lo resolvió todo: **«recargo la página y se queda cargando para siempre; cancelo y recargo 2 o 3 veces para que arranque»**. Una recarga que se cuelga no es cosa del navegador. **Medido con el panel bloqueado: el servidor respondía en 4 milésimas, 192 MB de RAM, 0,10 % de CPU, cero errores.** La causa es una cadena de tres cosas, ninguna evidente por separado: **(1)** nginx servía por **HTTP/1.1** (`listen 443 ssl`, sin `http2`), y en HTTP/1.1 **todos** los navegadores permiten solo **6 conexiones simultáneas por sitio** — de ahí que pasara igual en los dos. **(2)** El menú tiene 7 enlaces y Next.js **precarga esas páginas al pasar el ratón**, aunque no se haga clic. **(3)** Una de ellas, Base de Conocimiento, pedía `select('*')` sobre `products`, que incluye la columna `embedding` del buscador: **19.272 caracteres POR PRODUCTO**, o sea **675 KB y 0,83 s** por página, para datos que el panel no muestra nunca. Navegando entre pestañas, esas precargas ocupaban las 6 ranuras; sin ranura libre, **ni la recarga podía salir** — y cancelarla era lo que liberaba una. Y por eso **la consola no mostraba nada: no hay error, las peticiones están haciendo fila**. Arreglos: **HTTP/2** en nginx (de 6 conexiones a ~100 peticiones a la vez sobre una sola), la consulta del catálogo pidiendo las columnas una por una en vez de `*`, y el **Centro de Control**, que recorría dos tablas enteras paginando de a 1.000 **en fila** (3 páginas del catálogo y 11 de precios, unas 25 idas y vueltas), ahora pide todas las páginas **a la vez**. | `/etc/nginx/sites-enabled/knowledgebot` (copia previa en `/root/respaldos/`), `app/(app)/conocimiento/actions.ts`, `app/api/health/route.ts` | **HTTP/2 se aplicó con `reload`, sin reiniciar nada y sin tocar las pestañas abiertas**: `nginx -t` correcto y el sitio responde **HTTP/2 200 en 25 ms**. El dueño confirmó en el momento: «ya responde mejor, ya no se está colgando como lo hacía». Catálogo: **691.732 → 73.742 bytes** y **0,60 → 0,27 s** (9 veces más liviano). Centro de Control, las 11 páginas de precios: **2,70 s en fila → 0,61 s todas a la vez**, 4,5 veces más rápido; los datos son idénticos, cambia cómo se piden. Regresión del bot: banner laminado 200×100 **2/2 = $60.000**. | ✅ Desplegado y verificado |
| 2026-08-04 | **En el Pipeline, hacer clic y arrastrar se peleaban por la misma tarjeta.** El dueño: «estoy intentando entrar a un chat desde el pipeline y no hace nada, al igual que estoy intentando mover las tarjetas y no deja, como que se congela». **Dos causas, y la primera es mía.** **(1) Los despliegues.** Se reconstruyó la aplicación **siete veces en el día**. Cada `docker compose up -d app` deja las pestañas abiertas apuntando a una versión que ya no existe, y desde ahí ningún botón responde hasta recargar — es la regla 13 del proyecto, pisada todo el día. El contenedor arrancó a las 17:38; si el Pipeline estaba abierto de antes, no iba a funcionar nada. **De aquí en adelante: agrupar los cambios y desplegar UNA vez, avisando.** **(2) Un fallo real, que estaba desde siempre.** La tarjeta ENTERA era `draggable`. En un elemento arrastrable el navegador decide entre clic y arrastre según si el ratón se movió: **basta un píxel** entre apretar y soltar para que el clic se convierta en un arrastre que no llega a ninguna columna. Visto desde afuera es exactamente «no hace nada», y es **intermitente**, que es lo peor. Se separan los dos gestos, como en cualquier tablero: la tarjeta deja de ser arrastrable y aparece un **asa de seis puntitos** a la izquierda del nombre; se arrastra desde el asa y se hace clic en el resto. Además, **la tarjeta entera abre el chat** (antes solo el nombre), «Ver chat →» se ve siempre y no solo en pantallas anchas, y el botón de mover a otra columna deja de estar escondido: es la alternativa para quien no quiera arrastrar. | `app/(app)/kanban/client-page.tsx` | **Se descartó que fuera lentitud del servidor, con números:** la consulta del Pipeline tarda **0,16 s** para 142 conversaciones y devuelve 62 KB; la acción de mover una tarjeta son tres consultas cortas; **cero errores** en el registro. Tras desplegar con las pestañas cerradas: regresión del bot, banner laminado 200×100 **2/2 = $60.000**. El dueño usó además la X nueva y borró las ranuras que sobraban: el Centro de Control pasó de «7/8 activas» a **«1/2 conectadas · 1 requiere atención»**, que es el estado real. | ✅ Desplegado y verificado |
| 2026-08-04 | **Vencer un QR no es una decisión — y el Centro de Control llamaba «activas» a líneas que nadie había conectado.** Los dos fallos son propios, del arreglo de esta misma mañana. El dueño: «el problema de los QR persiste y el Centro de Control me sigue mostrando 2 de 3 líneas activas cuando yo solo tengo una conectada». **Causa 1:** el apagado automático del QR llamaba a la MISMA ruta que el botón «Desvincular», y esa ruta anota la línea como «apagada por el dueño». El dueño creó las ranuras 4 a 8, el panel les pidió el QR, venció a los 35 segundos y **las cinco quedaron anotadas como decisión suya sin que las tocara**. Medido en la base: `lineas_apagadas: [linea_2, linea_4, linea_5, linea_6, linea_7, linea_8]` cuando él solo había apagado `linea_2`. Ahora el vencimiento llama con `?intencional=0` y **solo el acto explícito de desvincular cuenta como decisión**. **Causa 2:** el rótulo decía «7/8 líneas activas» contando como activas las que solo estaban sin marcar. Activa significa **conectada** y nada más; si el panel muestra un 7 cuando hay una sola funcionando, deja de servir para lo único que sirve. Ahora dice «**1/8 conectadas**» y separa el detalle: «6 requieren atención · 1 apagada por usted». **Y la X que pidió el dueño:** «poner una X para eliminar o cerrar las líneas… y que no aparezcan más en el Centro de Control». Una ranura creada por error no se apaga: **se borra**. Cada tarjeta tiene ahora una X que elimina la ranura entera, con confirmación, deshabilitada mientras la línea esté conectada (primero hay que desvincular el número). **Las conversaciones NO se pierden:** viven en su propia tabla y siguen consultables aunque la ranura ya no exista. | `app/api/whatsapp-lines/[line_key]/route.ts`, `app/api/health/route.ts`, `app/(app)/integraciones/whatsapp/client-page.tsx` | Se limpió el estado que dejó el fallo: `lineas_apagadas` vuelve a tener solo `linea_2`, la única que el dueño apagó de verdad, y `hojas` y `persona` quedaron intactas. Con las 8 ranuras cargadas, el Centro de Control ahora informa **«1/8 conectadas · 6 requieren atención · 1 apagada por usted»** — que es exactamente el estado real: `linea_1` conectada, `linea_2` apagada por él, y seis ranuras sin usar. | ✅ Desplegado y verificado |
| 2026-08-04 | **«Logo» dejaba afuera media venta — y al corregirlo se me escapó la instrucción al cliente.** El dueño: «no creo que los clientes solo quieran poner logos; ¿qué tal un cliente que quiera Barbies en los botilitos para un cumpleaños?». Tiene razón y no es cosmético: una empresa manda su logo, un cumpleaños manda un personaje, un colegio manda un escudo. Con la palabra «logo» en el rótulo, el dueño podía dejar el campo vacío para una categoría que sí imprime cosas, solo porque no eran logos. Se cambió en la pantalla («El logo del cliente, ¿ya va en el precio?» → «**Lo que se imprime en el producto**, ¿ya va en el precio?») **y en el texto que recibe el bot**, que es donde de verdad cambia la respuesta. **Y ahí me equivoqué:** escribí la línea como una FRASE DECLARATIVA —«Lo que el cliente quiera imprimir en este producto YA ESTÁ INCLUIDO…»— y el modelo hizo lo único que se puede hacer con una frase así: **repetírsela al cliente palabra por palabra**, hablando de él en tercera persona. Medido: a «¿les puedo poner una foto a los cuadernos?» contestó *«Sí, claro que sí. Lo que el cliente quiera imprimir en este producto (su logo, un diseño, una foto, un personaje) ya está…»*. **Regla nueva, y vale para todo lo que se le entregue al modelo como guía:** que empiece por el hecho desnudo o por un verbo en imperativo, y que lleve **una orden explícita al lado**. Nunca una oración que funcione sola como respuesta. La versión anterior no fallaba porque remataba con «NO preguntes»; la nueva ahora dice «INCLUIDO EN EL PRECIO: … confírmaselo CON TUS PROPIAS PALABRAS … NUNCA le leas esta línea al cliente». | `app/(app)/conocimiento/KnowledgeBaseClient.tsx`, `lib/agent/hojas.ts` | **3 corridas de «¿les puedo poner una foto a los cuadernos?»**, todas en lenguaje propio: «Sí, claro que puedes ponerle una foto a los cuadernos», «A los cuadernos se les puede poner tu logo o la imagen que quieras», «Se puede poner una foto en los cuadernos». Ni rastro del texto interno. **Regresión: cuadernos con 6 insertos 2/2 = $487.000.** Aislado además que el fallo NO venía del cambio de «logo»: «quiero mugs con Mickey Mouse», «cotízame 6 mugs» y el banner de $60.000 respondieron bien durante toda la prueba. **Anotado aparte:** en dos corridas el bot contestó SOLO el saludo, con `finishReason: "other"` y el candado aprobando por «no-prices». Es el fallo de «se queda dormido» que ya está en la lista para el asesor — no es nuevo ni lo introdujo este cambio. | ✅ Desplegado y verificado |
| 2026-08-04 | **Los rótulos de las hojas, reescritos para quien no sabe dónde está parado — y cuatro avisos que faltaban.** El dueño, llenando su primera hoja: «alguien que no tenga ni idea de dónde está parado lee "con qué palabras se busca en el catálogo" y no va a tener ni idea de qué catálogo le hablan; esa tabla la creé yo para agilizar, pero no es algo establecido, en Colombia todavía se usa la hoja y el esfero». Tenía razón, y también en que «chuleta del asesor» no era un buen término. **Regla de redacción nueva para esta pantalla:** cada rótulo es una PREGUNTA que alguien puede contestar sin saber cómo funciona el sistema, y debajo va un ejemplo real. Fuera «chuleta», «catálogo» y «variante»; el encabezado ahora arranca por «Imagine que mañana entra alguien nuevo: ¿qué le explicaría el primer día?». «Con qué palabras se busca en el catálogo» pasa a «¿Con qué nombre lo tiene guardado usted?», con la ayuda diciendo dónde mirarlo. **Y los cuatro avisos, que salieron de sus preguntas.** Preguntó qué pasa si crea dos hojas con el mismo nombre, y destapó tres huecos: (1) dos hojas podían llamarse igual sin aviso; (2) peor, **dos hojas podían reclamar la misma categoría y el bot usaba una y descartaba la otra en silencio**; (3) y de las dos, **ganaba la MÁS VIEJA** — o sea que podía escribir una hoja entera, guardarla y no ver ningún efecto, creyendo que el bot no le hacía caso. Ahora manda la más nueva (las nuevas se agregan al principio y la primera que reclama una categoría se la queda) y los dos choques se avisan en amarillo, sin bloquear nada: a veces se quieren dos hojas parecidas. **(4)** Preguntó además cuántas hojas habría en diez años. El número no es el problema —se agrupan por cómo se cotiza, así que son diez o quince— pero **una hoja muerta se veía igual que una viva**. Cada hoja muestra ahora **a cuántos productos llega**, y una en cero avisa que el bot nunca la va a usar. **De paso, un error propio:** la nota de ejemplo de Cuadernos decía «se cotiza con `buildQuote`» — jerga de programador en un campo que es del dueño. Reescrita en español. | `app/(app)/conocimiento/KnowledgeBaseClient.tsx`, `app/(app)/conocimiento/actions.ts`, `lib/agent/hojas.ts` | Compila sin errores de tipos. Tras desplegar, la hoja de Cuadernos sigue completa (2 categorías, marcación incluida) y `persona` y `lineas_apagadas` intactas. **Regresión de la pregunta que perdió la venta el domingo:** «quiero 20 cuadernos argollados 120 hojas y ya vienen marcados?» → *«La marcación del logo ya está incluida en el precio»* y **una sola pregunta**, el tamaño. El conteo se calcula paginando de a 1.000 y pidiendo solo `category_id`: traer los productos enteros arrastraría la columna del buscador, 19 KB por producto. | ✅ Desplegado y verificado |
| 2026-08-04 | **Cinco botones que nunca tuvieron estilo, y uno que parecía no funcionar.** El dueño, entrando por primera vez a las hojas: «agregar hoja no sirve y se ve raro este botón, no parece un botón sino más bien un texto escrito». Las dos cosas eran ciertas y las dos eran del mismo error. **La clase `btn-secondary` NO EXISTE en el proyecto** — `app/globals.css` solo define `.btn-primary` y `.btn-ghost`. Sin la clase, el navegador no aplica nada: el botón pierde el fondo, el borde y hasta la fila, así que el icono se va arriba del texto y queda pareciendo un título suelto. Se usaba en **cinco botones de dos pantallas**: «Agregar hoja» y «Agregar cuenta» en Conocimiento y Personalización, y «Agregar sede» y «Agregar teléfono», que se escribieron ayer con el mismo defecto sin que nadie lo notara. Se crea la clase, y los cinco quedan arreglados de una vez. **Y lo de «no sirve» era otra cosa:** el botón sí funcionaba, pero agregaba la hoja nueva **al final de la lista**; con la hoja de Cuadernos ya en pantalla, el dueño hacía clic y no veía nada. Ahora la hoja nueva aparece **primera**. **Se evitó un daño de paso:** en la captura se veía que estaba editando la hoja de Cuadernos y le había cambiado el nombre a «llaveros» —los campos de abajo seguían diciendo «cuaderno 80 hojas», «cosido», «agenda»—. Se le avisó antes de que guardara: la hoja de Cuadernos sigue intacta. | `app/globals.css`, `app/(app)/conocimiento/KnowledgeBaseClient.tsx` | La clase existe y los 5 botones la toman. La hoja nueva sale arriba. Verificado tras desplegar que `metadata.hojas` sigue con la hoja de Cuadernos completa (`buscar_como: cuaderno 80 hojas`, `marcacion: incluida`) y que `persona` y `lineas_apagadas` no se tocaron. | ✅ Desplegado y verificado |
| 2026-08-04 | **El QR que se generaba para siempre, y las alarmas falsas del Centro de Control.** Reportado por el dueño: «no es bueno que se quede generando QR toda la vida» y «si uno cancela o desvincula una línea no debería aparecer como un problema». Las dos causas, medidas. **1) El QR infinito:** el panel preguntaba por el código cada 3 segundos mientras la línea estuviera en `awaiting_qr`, y **cada pregunta despierta la sesión en el puente** (`startSession(line, 'qr-solicitado')`, con su propio registro: «QR solicitado desde el panel: se despierta la línea»). Como **nada sacaba nunca a la línea de ese estado**, el ciclo no terminaba jamás. El vencimiento de 35 s ya se calculaba, pero solo cambiaba lo que se ve en pantalla. Ahora, al vencer, se desvincula de verdad: se cierra la sesión en el puente y la línea queda en reposo hasta que alguien vuelva a pedir el QR. **2) Las alarmas falsas:** el Centro de Control contaba como problema **toda** línea no conectada. Con la meta de 8 líneas, cada ranura creada y todavía no usada es una alarma permanente por decisión propia — el panel en rojo porque sí. **La señal es el acto, no una casilla nueva:** desvincular ES declarar que no se quiere esa línea conectada, y pedirle el QR es la declaración contraria; el sistema no adivina, lee lo que el dueño ya hizo. Se anota en `agent_configs.metadata.lineas_apagadas`, el mismo sitio que las hojas y las sedes, porque la columna `status` de `whatsapp_lines` es un **enum cerrado** y rechaza un valor nuevo (se probó: `invalid input value for enum whatsapp_line_status: "pausada"`). **3) Lo que NO se tocó, porque ya estaba bien:** el puente detecta la restricción de WhatsApp, la guarda en disco con su fecha de fin y **se niega a enviar** con un 503 explicativo mientras esté vigente. Esa protección ya existía y funcionó — la línea restringida no siguió intentando. | `lib/whatsapp/lineas-apagadas.ts` (nuevo), `app/api/whatsapp-lines/[line_key]/route.ts`, `app/api/whatsapp-lines/route.ts`, `app/api/health/route.ts`, `app/(app)/integraciones/whatsapp/client-page.tsx` | `linea_2` estaba en `awaiting_qr` generando códigos sin parar: se le cerró la sesión en el puente y quedó en `disconnected`. Centro de Control: **antes 2 líneas marcadas como problema, ahora 1** — `linea_2` figura como «Apagada por usted» y solo queda `linea_3`, que el dueño no apagó. **Sobre la restricción de esa cuenta:** WhatsApp declaró `RESTRICT_ALL_COMPANIONS` hasta el **11-ago-2026**; restringe los dispositivos vinculados, **no el teléfono**. Y **no fue por volumen**: en todo el registro disponible esa línea tiene **3 eventos**. Regresión del bot: banner laminado 200×100 **2/2 = $60.000**. | ✅ Desplegado y verificado |
| 2026-08-04 | **Las hojas de categoría: la chuleta del asesor sale del código y pasa al panel.** Hasta hoy el sistema tenía **UNA sola hoja de 82 categorías**, la de cuadernos, escrita a mano DENTRO del prompt («REGLA ESPECIAL: Cuadernos — necesitas 4 datos: tamaño, hojas, cantidad, argollado o cosido»). Funcionaba, pero el dueño no podía verla ni cambiarla, y las otras 81 categorías **no tenían ninguna** — por eso el bot improvisaba, y cuando improvisa, interroga. Medido el 03-ago: a «¿y ya vienen marcados?» sobre cuadernos pidió tamaño del logo, después técnica, después número de tintas —tres turnos— y terminó en «hubo un error al cotizar». Cinco mensajes, cero venta. **La hoja distingue dos cosas que estaban mezcladas:** *qué necesita saber el asesor* (cuántas tintas) y *cómo pedírselo al cliente* (¿tu logo es de un solo color o tiene varios?). El cliente no tiene por qué oír nunca la palabra «tinta». **Una hoja puede cubrir VARIAS categorías** — es la respuesta a «¿qué más se puede combinar?»: se agrupa por qué hay que preguntar para cotizar, no por lo que el producto es. Y hay una **hoja general** para todo lo que no tenga hoja propia. **Dónde vive:** `agent_configs.metadata.hojas`, el mismo patrón que las cuentas de pago, los temas y las sedes — sin tabla nueva y sin migración. **Cómo llega al bot:** `searchCatalog` le adjunta a cada producto la guía de su categoría en `guia_de_venta`. Es **dato de recuperación, no una instrucción metida en el prompt** (la R de RAG), y **llena el hueco de `instrucciones_venta`** — el campo que el prompt mandaba obedecer dos veces y que nunca existió ni como columna ni en el buscador. **Lo vacío se omite:** un campo en blanco no se rellena con un ejemplo; el bot no dice nada de eso y ofrece consultarlo. Se cargó la hoja de **Cuadernos** copiando literalmente lo que estaba en el prompt, como ejemplo vivo para que el dueño arme las demás. | `lib/agent/hojas.ts` (nuevo), `lib/agent/tools/search-catalog.ts`, `lib/agent/index.ts`, `app/(app)/conocimiento/actions.ts`, `app/(app)/conocimiento/KnowledgeBaseClient.tsx` | **La prueba que importa, la pregunta que perdió la venta el domingo:** «quiero 20 cuadernos argollados 120 hojas **y ya vienen marcados?**» → *«Sí, la marcación del logo ya está incluida en el precio»* **y una sola pregunta** (el tamaño). Antes: tres preguntas y un error. **Regresión: cuadernos con 6 insertos, 3/3 = $487.000.** Sin errores cargando hojas en el registro. Respaldo previo: etiqueta `respaldo-antes-de-hojas` en `7a514db`, más copia de `agent_configs`, `categories`, `system-prompt.ts` e `index.ts` en `/root/respaldos/`. | ✅ Desplegado y verificado |
| 2026-08-03 | **El grafo sabía QUÉ existe pero no PARA QUÉ sirve. Ahora los mecanismos se buscan por su nombre.** El dueño lo pidió así: «no tener que acudir al código para saber que hay una memoria que existe y que sirve para X cosa». Tenía razón, y **la causa se midió**: de los 102 nodos de razonamiento del grafo, **93 salen de Python y solo 4 de TypeScript** — y el sistema es TypeScript en un 95 %. `lib/agent/index.ts`, 1.400 líneas con los comentarios más importantes del proyecto, aporta al grafo **solo nombres**: sabía que `photosByConversation` existe, no para qué sirve. El motivo es que el extractor convierte los *docstrings* de Python en nodos de razonamiento y no hace lo mismo con los comentarios de bloque de TypeScript; y la capa semántica —la que sí lo haría, con LLM— **nunca se ha corrido en este proyecto**: el grafo es 100 % extracción de estructura (no existen `.graphify_semantic.json` ni `.graphify_extract.json`). **La palanca que sí funciona hoy:** cada título de la documentación **sí** se vuelve un nodo del grafo. Se comprobó — de `docs/ESTADO_OPERATIVO.md` salían 47 nodos, uno por encabezado. Así que se escribió **§12, «Índice de mecanismos»**, con un título propio por cada pieza y, debajo, qué es, dónde vive y qué se midió de ella: la memoria de fotos, el historial de 10 mensajes, el repartidor de fotos que falla en silencio, el filtro de identidad y su agujero, `respuestaSirve` que existe y no se usa donde hace falta, el buscador de las fichas, las sedes, `buildQuote`, el candado y sus motivos, `instrucciones_venta` que no existe, el diccionario de jerga que está escrito y no se cumple, y las rejillas de precio. **Y se avisó en `AGENTS.md`**, con la regla para el que venga: cuando descubras cómo funciona algo, no lo dejes solo en un comentario del código — ponele título en §12, o el siguiente tendrá que releer 1.400 líneas para saber lo mismo. **Honestidad sobre el alcance:** los nodos nuevos tienen grado 1 — cuelgan de su sección, **no están enlazados a las funciones del código**. Enlazarlos exige correr la capa semántica, que necesita una llave de LLM que hoy no está configurada. | `docs/ESTADO_OPERATIVO.md`, `AGENTS.md` | `docs/ESTADO_OPERATIVO.md` pasa de **47 a 60 nodos** en el grafo (los 13 títulos nuevos), `AGENTS.md` de 10 a 11, y el grafo entero de 1.391 a **1.405 nodos y 2.455 aristas**. Probado que se encuentran por su nombre: `graphify explain "El repartidor de fotos"`, `"La memoria de fotos por conversación"` y `"La cotización armada"` devuelven los tres. `graphify diagnose multigraph` sin duplicados. El diseño del dueño se le volvió a aplicar al grafo con `scripts/aplicar_tema_grafo.py`. | ✅ Hecho y verificado |
| 2026-08-03 | 🔴→✅ **EL TOTAL TAMBIÉN PASA POR LA CALCULADORA. Cerrado el fallo abierto A.** El dueño lo pidió con estas palabras: «los precios deben obligatoriamente pasar por una calculadora sin importar el valor o resultado». Su regla era correcta y estaba a medias: **se aplicaba a cada precio suelto pero no al total**. Un cuaderno no tiene un precio, se arma sumando piezas (base + insertos + filtro UV + guardas); cada pieza salía bien de la calculadora, pero **la suma y la multiplicación las escribía el modelo de cabeza**, y esa es la única cifra que le llega al cliente. Reproducido hoy, cinco corridas de la misma frase con contacto nuevo: **$487.000 ✔, $487.000 ✔, $427.000 ✘, $584.000 ✘ y una que no cotizó**. $157.000 de diferencia en el mismo pedido. Las dos malas, desarmadas: **$427.000** = `(16.000 + 5.350) × 20`, se le olvidaron 2 de los 6 insertos; **$584.000** = `(16.000 + 10.200 + 3.000) × 20`, partió el 6 como **8+2 = diez insertos**, y encima lo escribió («con 10 insertos»). **Y el prompt YA decía, con estas palabras, «desglosa de MAYOR a MENOR: 6 = 4+2».** Estaba escrito y usó 8+2 igual: la regla del proyecto demostrada otra vez sobre sí misma. **Herramienta nueva `buildQuote`:** el modelo dice QUÉ lleva el pedido, sin una sola cifra; el código resuelve el desglose leyendo del catálogo qué presentaciones existen (no hay ninguna lista escrita: los insertos son 1, 2, 3, 4 y 8 porque así están cargados, y si mañana se carga uno de 6 lo usa solo), busca cada tarifa, suma y multiplica. **No adivina:** si el tamaño es ambiguo devuelve las opciones para que se le pregunte al cliente, en vez de elegir entre $15.000 y $23.000 por cuaderno. **Candado ampliado:** hasta hoy comprobaba de dónde salía cada precio y, una vez usada la calculadora, **dejaba pasar cualquier cifra**. Ahora la cifra **más alta** de la respuesta —siempre el total, y la única que decide lo que paga el cliente— tiene que ser una que alguna herramienta de precio produjo en el turno, o una ya aprobada antes, o un monto del panel. Solo la más alta a propósito: el unitario y el anticipo son divisiones legítimas y exigirlas todas bloquearía respuestas correctas. **De paso, el prompt quedó más corto:** se le quitó la instrucción del desglose (ahora la hace el código) y **las dos órdenes de obedecer `instrucciones_venta`, que no existe** — no es columna de `products` ni la devuelve el buscador, así que se le mandaba obedecer un papel que nunca llegaba, justo el escenario que le hace inventar. | `lib/agent/tools/build-quote.ts` (nuevo), `lib/agent/index.ts`, `lib/agent/system-prompt.ts` | **Las cinco corridas del cuaderno: 5/5 = $487.000** (antes 2/5). **7 insertos, 3/3 = $512.000** — el desglose 4+3 verificado a mano contra las tarifas (16.000 + 5.350 + 4.250 = 25.600 × 20). **Regresión banner laminado 200×100: 3/3 = $60.000.** Y lo que más se vigilaba, que el candado nuevo no bloquee de más: **6 mugs → tres opciones a $72.000**, **1000 llaveros → tres opciones**, **500 llaveros manilla a 2 tintas → $1.050.000** (tarifa $2.100 × 500, verificada contra el Excel del dueño). **Ningún bloqueo espurio: todas aprobadas en el intento 1.** | ✅ Desplegado y verificado |
| 2026-08-03 | **Varias sedes, cada una con sus teléfonos, y cada teléfono diciendo si timbra o solo recibe mensajes.** Pedido del dueño: «agranda el formulario y dale la opción de crear nuevas direcciones y nuevos números para llamar o para mensajear por WhatsApp». Hasta hoy el panel tenía **un** campo de dirección y **uno** de teléfono, y la empresa tiene más de cinco sedes: el bot solo podía dar una, siempre la misma, sin importar cuál le sirviera al cliente. Ahora `business_info.sedes` es una lista sin límite —el mismo patrón que ya usaban las cuentas de pago— y dentro de cada sede otra lista de teléfonos, cada uno marcado **llamadas y WhatsApp**, **solo llamadas** o **solo WhatsApp**. Esa marca no es un adorno: sin ella, a «¿a qué número llamo?» el bot puede dar un número que solo recibe mensajes y mandar al cliente a un teléfono que nunca va a timbrar. **La dirección y el teléfono que ya estaban se convierten solos en la primera sede** al abrir el panel, y al guardar, `address` y `phone` se vuelven a derivar de ella: hay una sola verdad y nada que sincronizar a mano. **Dos cosas se destaparon probando, y las dos eran del mismo tipo — el dato estaba bien y la forma de decirlo lo arruinaba:** (1) con dos sedes cargadas, a «¿dónde quedan ubicados?» el bot contestó con **una sola**, aunque el registro muestra que `queryKnowledgeBase` le había devuelto la lista entera; la culpa era del **encabezado del prompt**, que nombraba una dirección suelta, y un dato puesto arriba pesa más que una ficha recuperada. Con dos o más sedes el encabezado ya no nombra ninguna —**el prompt queda más corto, no más largo**— y la única fuente pasa a ser la lista. (2) La ficha decía «para llamar X, por WhatsApp Y» y el modelo lo leyó como una sola bolsa: remató con «también puedes escribirnos por WhatsApp a esos mismos números», incluyendo el fijo. Ahora los grupos se nombran **«solo llamadas, NO recibe WhatsApp»** y **«solo WhatsApp, NO recibe llamadas»**, sin mezclar. Si una frase se puede leer de dos maneras, el modelo elige una, y no siempre la correcta. **Lo que falta se omite y no se rellena:** una sede sin teléfono se dice sin teléfono, y un número sin tipo se ofrece a secas sin afirmar para qué sirve. | `app/(app)/personalizacion/client-form.tsx`, `lib/personalization/actions.ts`, `lib/agent/tools/query-knowledge-base.ts`, `lib/agent/system-prompt.ts` | **22/22** de una batería nueva —cómo se cuenta una sede (con fijo y WhatsApp, con un número para las dos cosas, sin teléfono, sin dirección, sin tipo, vacía) y diez formas de preguntar por sedes— más **17/17** de la batería del buscador, que no empeoró en ningún caso. En vivo, con **dos sedes de prueba cargadas en la base y devueltas al respaldo exacto al terminar** (verificado byte a byte): a «¿dónde quedan ubicados?» responde **las dos sedes con su dirección y el tipo de cada número**; a «¿a qué número puedo llamar?» da **solo los que reciben llamadas**, no el de solo-WhatsApp; a «¿dónde queda la sede norte?» responde esa. **Regresión: banner laminado 200×100 = $60.000.** | ✅ Desplegado y verificado |
| 2026-08-03 | **El bot negaba tener página web teniéndola guardada en el panel. No era el bot: era el buscador de las fichas.** Pedido del dueño (problema 5a): averiguar por qué el sitio web no le llega al bot. Reproducido por el simulador: a «¿tienen página web?» contestó ***«No tenemos una página web como tal»*** — una negación rotunda de un dato que **sí** estaba en `agent_configs.business_info` (`https://zoompublicidad.com.co/`). **El registro descarta las dos hipótesis fáciles:** el bot **sí** llamó a `queryKnowledgeBase`, y **sí** con la consulta correcta («pagina web»); lo que faltó fue el resultado. La causa está en `buscarEnFichas`, en dos defectos que se suman: (1) descartaba **toda palabra de 3 letras o menos** —y la palabra era **«web»**—, con lo que se quedaba buscando «pagina», que no aparece ni en el título «Sitio web» ni en la URL: cero resultados, y el modelo, sin dato, concluyó que no había web; (2) comparaba **en un solo sentido** (¿cabe el término dentro del título?), así que un plural nunca encontraba a su singular — por eso ese mismo día, a «dame direcciones y números de contacto», devolvió teléfono y correo y **se saltó la dirección**, que estaba guardada. Medir las palabras por longitud descarta justo lo que importa (web, iva, nit, pdf) y deja pasar lo que no (para, como, esta): ahora hay una **lista nombrada de palabras vacías** y una función `raiz()` que quita el plural **de los dos lados**. El título se compara **palabra por palabra**, no como un solo texto, para que una raíz corta no pueda colarse dentro de otra palabra cualquiera. **No es un arreglo de «la página web»: es del buscador**, y por eso destapó tres campos más del panel que también se perdían según cómo preguntara el cliente. | `lib/agent/tools/query-knowledge-base.ts` | Batería de **17 casos** sobre las fichas del panel, corrida contra el código **de antes y el de después**, extraídos los dos del archivo real: **antes 12/17, ahora 17/17**, sin un solo caso que empeore. Los 5 que fallaban: «tienen pagina web?», «pasame la web porfa», «dame direcciones y numeros de contacto», «que garantias manejan» y «cuales son sus telefonos» — los tres últimos **no se habían pedido**, salieron solos de la misma causa. Por el simulador, con contacto nuevo cada vez: *«nuestra página web es https://zoompublicidad.com.co/»* y *«Dirección: Cra 28 #10 -86 Edificio Fénix (Local 104) · Teléfono: +57321 201 6229»*. **Regresión obligatoria: banner laminado 200×100 = $60.000.** | ✅ Desplegado y verificado |
| 2026-08-03 | **El manual y el grafo del código, accesibles desde el panel — cada uno con su puerta.** Pedido del dueño: los dos archivos HTML del proyecto tenían que poderse ver desde el panel, el grafo «de forma un poco oculta» y el manual «más accesible». **No van en `public/`**: lo que se pone ahí queda abierto a internet, sin sesión, y ni el grafo ni el manual tienen por qué estarlo. Se sirven por `/api/documentos/[archivo]` con **lista blanca de dos nombres** —así no hay forma de pedir `../.env.production` ni ningún otro archivo del servidor— y cada uno con su portero: **manual** → cualquiera con sesión, y va en el **menú principal**, con su nombre, porque es para quien usa la plataforma; **grafo** → solo el rol `owner`, y a cualquier otro le responde **404**, así que ni se entera de que existe. En pantalla, el grafo va en el pie del sidebar con el **mismo tono casi invisible** que ya usaba el Centro de Control: el patrón de «controles técnicos ocultos» ya existía y se reutilizó tal cual. **Se montan como volumen, no se copian a la imagen**, porque el grafo se regenera con cada `graphify update`: así el panel siempre muestra el último sin reconstruir nada. El manual estaba solo en el PC del dueño y se subió al VPS (3,9 MB). | `app/api/documentos/[archivo]/route.ts` (nuevo), `docker-compose.yml`, `components/sidebar.tsx` | Los dos archivos llegan al contenedor (manual 3,9 MB, grafo 1,1 MB). **Sin sesión: manual 401, grafo 401.** Un nombre inventado (`/api/documentos/env`): **404**, la lista blanca funciona. Build sin errores de tipos; simulador sin regresión: banner laminado = $60.000. | ✅ Desplegado |
| 2026-08-03 | **El bot deja de pedir datos de una marcación que no sabemos cotizar.** Captura del dueño: el cliente preguntó «¿y ya vienen marcados?» y el bot abrió un camino sin salida — le pidió el **tamaño del logo**, luego la **técnica**, luego el **número de colores** (tres turnos) y terminó en *«Disculpa, parece que hubo un error al intentar cotizar la tampografía»*. Cinco mensajes gastados y cero venta. **La causa está en los datos, no en el modelo:** el catálogo **no tiene ninguna marcación para cuadernos**. Los únicos servicios que existen son bordado y **dos tampografías atadas a otro producto** («sobre manilla lisa», «sobre llavero»). El modelo se lo inventó con su conocimiento general de la industria. Lo que sí existe y sí tiene tarifa para un cuaderno son los adicionales —insertos, filtro UV, guardas, cosido, diseño, los 13 productos con 20-21 tarifas cada uno—, y **eso es lo que hay que ofrecer**. Candado nuevo: si la respuesta pide un dato de marcación (técnica, tintas, tamaño del logo) y `searchCatalog` **no devolvió ningún servicio de marcación** en el turno, se bloquea y se le ordena ofrecer los adicionales que sí existen, diciendo en una línea que la marcación se confirma aparte. Si el catálogo **sí** devolvió uno (una tampografía sobre llavero, un DTF, un bordado), preguntar está bien y pasa. Es el mismo principio de `DEFAULT_PERSONA`: si el dato no está, no se inventa. | `lib/agent/index.ts` | El candado **se vio disparar** en el registro: «GUARDRAIL: pide datos de una marcación que el catálogo no cotiza», con la lista de lo que sí devolvió el catálogo. Reproducida la conversación de la captura: ya **no** pide técnica, tintas ni tamaño de logo. **Destapó un choque entre candados y se corrigió:** la orden de corrección manda buscar los adicionales, y eso disparaba el candado de «buscó otra cosa» (el cliente había escrito «vienen marcados»); los dos se peleaban, se agotaban los tres intentos y el cliente recibía la respuesta de respaldo. Ahora, tras ese bloqueo, el candado de tema se calla: lo que busca es exactamente lo que se le mandó buscar. | ✅ Desplegado y verificado |
| 2026-08-03 | **El bot deja de contar cómo funciona por dentro, y la cotización se dice en una línea.** Captura del dueño: *«Disculpa, parece que hubo un problema y no se adjuntó la foto. El sistema se encarga de enviarlas automáticamente.»* Dos cosas mal en dos renglones — suena a máquina, y además, en palabras del dueño, **si la foto se envió, decirlo sobra**: no le sirve al cliente y gasta tokens en cada mensaje. **La causa estaba en el propio prompt**, que le enseñaba la frase: decía «El envío de la imagen lo hace el sistema». El bot repetía su instrucción al cliente. Se quitó esa frase (el prompt queda **más corto**, que es el sentido correcto según la regla del proyecto) y se añadió al filtro determinista la tercera familia, **la fontanería**: el bot contando cómo se entregan los mensajes. La marca es la **voz impersonal sobre el mecanismo** —«se adjuntó», «se envían solas», «el sistema se encarga»—; un vendedor habla en primera persona —«te la mando», «te adjunto el diseño»— y eso pasa entero. Ese es el discriminante. Además, «el sistema» a secas pasa a ser marca de máquina, salvo cuando lleva «de» detrás («el sistema de impresión DTF», «sistema de cierre hermético»), que es lenguaje de producto: antes era una lista de verbos y por eso se le escapó «se encarga de». **Y la cotización:** la plantilla del prompt pedía cinco renglones (base, adicional, unitario, total). Ahora es **una sola línea** con cantidad, producto, referencia y total; el desglose solo si el cliente lo pide. | `lib/agent/index.ts`, `lib/agent/system-prompt.ts` | **26/26** casos del filtro: las 11 frases que debe tapar (las dos de la captura incluidas) y las **15 que NO debe tocar** — «Claro, te la mando ahora mismo», «Te adjunto el diseño», «el sistema de impresión DTF», «la lámpara se apaga automáticamente», «el dispensador funciona de forma automática». Destapó un fallo propio: `envi` no casa con «envían», que lleva tilde; corregido a `env[íi]`. Por el simulador, pidiendo una foto: **«Claro, te la mando ahora mismo. Es un cuaderno argollado…»**, sin una palabra de máquina. Cotización: banner laminado 200×100 → **una línea, $60.000**. | ✅ Desplegado y verificado |
| 2026-08-03 | **El desglose de insertos es cuenta nuestra, no del cliente — y se arregla en código, no pidiéndoselo al modelo.** El dueño: «dividió los insertos para el cliente y eso no es lo correcto». El catálogo no tiene un producto de «6 insertos»: el precio se arma sumando 4 + 2, y el prompt manda desglosarlo **para calcular**. Pero eso se le colaba al cliente: *«con 4 insertos (Ref: ZM-CUA-011) y 2 insertos (Ref: ZM-CUA-002)»*. **Primero se intentó por el prompt** —se le añadió «el desglose es para calcular, no para contar»—, se desplegó y **siguió partiéndolos igual**. Es la regla del proyecto demostrada sobre sí misma: lo que obliga son los guardrails deterministas, no las instrucciones. Se resolvió con `unificarInsertos`, que une los pares en la respuesta ya aprobada, junto al formato de las cifras. Solo une lo que está en la **misma línea** (`[ \t]`, nunca `\s`): una lista de opciones en renglones distintos son dos opciones, no una suma. | `lib/agent/index.ts` | **8/8** casos: une 4+2, 4+3, 8+2 y la forma con coma, con y sin referencias; y **no toca** «insertos de 1, 2, 4 y 8 unidades», «lleva 4 insertos», ni una lista en renglones. Por el simulador: *«20 Cuaderno Argollado - Base 120 hojas **con 6 insertos** — $487.000 COP»*. | ✅ Desplegado y verificado |
| 2026-08-03 | 🔴 **HALLAZGO GRAVE: el mismo pedido da totales distintos.** Salió al probar lo anterior. Misma pregunta, contacto nuevo, tres corridas: **$487.000, $487.000 y $415.000**. La correcta es $487.000 (base $16.000 + 4 insertos $5.350 + 2 insertos $3.000 = $24.350 × 20). En la corrida mala el modelo usó tarifas equivocadas de los adicionales ($60.000 y $35.000 por los 20, cuando son $107.000 y $60.000) y **nadie lo detectó**: el candado antialucinación comprueba **de dónde salió cada precio**, no si la suma está bien ni si las tarifas eran las del pedido. Un cliente puede recibir una cotización **$72.000 por debajo (15 %)** sin que salte nada. Es el fallo abierto A —«el total combinado»— con números. **No se corrigió aquí a propósito**: hace falta una «cotización armada» que junte los conceptos y **pase el total por la calculadora**, y mezclarlo con lo demás habría dejado las dos cosas a medias. | — | Tres corridas del simulador con contactos nuevos, misma frase: 2 × $487.000 y 1 × $415.000. | 🔴 Abierto — es la siguiente tarea que importa |
| 2026-08-03 | **La causa de las caídas: el bot pedía sin parar los ESTADOS que no puede leer. La basura ya no entra, se rechaza en la puerta.** El dueño no aceptó el parche («desconectando y reconectando se soluciona, pero **quiero evitarlo**, ¿qué es lo que se traba?») y tenía razón: re-vincular una línea arregla esa línea, no la clase de fallo. **Lo que se traba son los estados** (las historias que publican los contactos del número) y algún mensaje de grupo. WhatsApp se los manda a **todos** los dispositivos vinculados, incluido el bot. El bot **no puede abrirlos** —no tiene la llave de esa persona y nunca la va a tener— así que hace lo único que sabe: **pedirle a WhatsApp que se los reenvíe**. WhatsApp los reenvía, vuelve a fallar, los pide otra vez. Para siempre. **El contraste que lo prueba**, medido sobre 3 h de registro: `linea_3` → **7 fallos desde `status@broadcast` + 1 de grupo**, cada uno con su «sent retry receipt», **ninguno llegó nunca** — y es la línea que se cae cada 50m04s. `linea_1` → **26 fallos, todos de UNA persona real** (`@lid`): esos **sí** se resuelven (falla → pide reenvío → «Mensaje recibido» al segundo intento) — y esa línea **no se cae**. Lo de una persona se cura solo; **un estado no se cura nunca**, se queda pendiente del lado de WhatsApp, y cada ~50 min WhatsApp termina el flujo. **Lo absurdo:** el sistema YA tiraba esos chats (178 descartados: 85 difusiones y estados, 84 grupos, 9 canales), pero el filtro estaba en `handleIncoming`, que corre **después** de que la librería intentó abrirlos y pidió el reenvío. **La regla correcta en el lugar equivocado — el mismo error que con los stickers.** Arreglo: se usa `shouldIgnoreJid` de Baileys con la **misma** función `esChatDePersona` (no una segunda lista que pueda desincronizarse). Al ignorar un chat, la librería **le acusa recibo a WhatsApp y no lo abre** (`messages-recv.js:610`, y lo mismo para avisos en :578 y acuses en :500): ese acuse es lo que hace que WhatsApp lo dé por entregado y **deje de reenviarlo**. **Ante la duda, pasa:** sin `jid` no se ignora nada, porque dejar entrar de más solo ensucia y dejar fuera de más pierde un cliente en silencio. Y el descarte **sigue sin ser mudo**: se cuenta por dominio en `/diagnostic` y se anota en el registro. **Además, cinturón y tirantes:** el puente detecta ahora que una línea se cae **en ciclo** (tres cortes con huecos parecidos, entre 40 y 70 min) y el panel de Líneas lo muestra arriba de la tarjeta con la causa y la reparación. Ese aviso hacía falta porque **el fallo es invisible**: la línea cae 4 segundos y vuelve sola, así que el panel siempre decía «conectada». **Nunca desvincula solo**: desvincular obliga a que una persona escanee un QR, y hacerlo por su cuenta dejaría un local mudo sin que nadie se entere. | `wa-server-baileys/server.js`, `app/api/whatsapp-lines/diagnostic/route.ts`, `app/(app)/lineas/lineas-client.tsx` | Verificado en el código de la librería que el camino de ignorar **acusa recibo y no descifra** (`messages-recv.js:610-615`). Tras desplegar: **0 «failed to decrypt» y 0 «sent retry receipt»** donde antes había 34 en 3 h, y la puerta rechazando (2 chats en 8 min, contados en `/diagnostic`). Las 3 líneas reconectaron en **3 segundos**. Simulador sin regresión: banner laminado 200×100 = **$60.000**. **La prueba de fondo es el tiempo:** hay que ver a `linea_3` pasar 2 h sin caerse. | ⚠️ Desplegado y verificado en lo inmediato; **la prueba de las 2 h queda pendiente** |
| 2026-08-02 | **La flecha de responder tapaba el texto en el teléfono.** Reportado por el dueño con una captura, el mismo día que se entregó. La flecha se puso **dentro** de la burbuja, arriba a la derecha, apareciendo al pasar el ratón por encima. En el computador se veía bien. **En el teléfono no existe «pasar por encima»**, así que ahí tenía que verse siempre — y siempre significaba tapando el primer renglón de cada mensaje. Se saca al **hueco que queda al costado**: a la izquierda si el mensaje es nuestro, a la derecha si es del cliente; siempre por el lado libre. Ocupa su sitio desde el principio y solo se atenúa, en vez de esconderse, así que al pasar el ratón por encima **nada se mueve de lugar**. En el teléfono queda tenue. **La lección, para no repetirla:** un control que en el computador se revela al pasar el ratón, en el teléfono se ve siempre — hay que mirarlo en el sitio donde no hay ratón antes de darlo por bueno. | `components/chat/message-bubble.tsx` | Build sin errores de tipos; el panel levanta y responde 200, sin errores en el registro. La comprobación en pantalla la hace el dueño, que es quien lo vio. | ✅ Desplegado |
| 2026-08-02 | **La foto tomada con el teléfono no salía del panel: «error 413». No era el sistema, era el portero.** El dueño lo probó y lo reportó en el momento. **No es un fallo del bot ni de WhatsApp:** nginx, que está delante del panel, aplica su tope por defecto de **1 MB** cuando no se le declara ninguno, y rechaza la petición **antes de que llegue al sistema** — así que el panel no puede ni explicarlo, solo muestra el número pelado. Una foto de celular pesa entre 3 y 8 MB. Esto explica por qué el envío de fotos desde el panel, desplegado ese mismo día, había quedado como «pendiente de prueba del dueño»: **por el dominio nunca funcionó**, aunque el código estuviera bien. Se comprobó además que **los mensajes ENTRANTES nunca estuvieron en riesgo**: el puente le habla al sistema por `localhost:3003`, sin pasar por el portero. Se pone el tope en **25 MB** —y no en 20— para que quien avise cuando alguien se pase sea el sistema, con su frase clara, y no un 413 del portero. Va en los dos bloques de nginx, incluido el del puerto 80, donde vive el webhook. **De paso salva las notas de voz largas**, que también pasan del megabyte. | `/etc/nginx/sites-enabled/knowledgebot` (copia previa en `/root/pruebas/knowledgebot.nginx.antes`) | El registro de nginx con el número exacto: `client intended to send too large body: **2875561 bytes**` sobre `POST /api/conversations/send-media`, desde Chrome de Android. `nginx -t` correcto y recarga **en caliente** (`reload`, no `restart`): ni el sistema ni el puente ni las líneas se tocan. Prueba real subiendo **3 MB** por el mismo camino: **antes 413, ahora 401** — el archivo entra completo y lo rechaza el sistema por falta de sesión, que es lo correcto para una petición hecha desde fuera. | ✅ Corregido y verificado |
| 2026-08-02 | **Las caídas cada 50 minutos: la hipótesis se midió y era FALSA. Queda escrita la causa real.** Punto de partida (medido sobre 5 h): `linea_3` se cayó 7 veces y `linea_1` dos, con **50m04s exactos** entre una y otra. El reloj **no estaba en el servidor** —ni temporizador, ni cron, ni systemd—: cada 50m04s WhatsApp manda `{tag:'stream:error', attrs:{}, content:[{tag:'ack', class:'message', type:'media', id:'3A0DF74A8F30B473DED8'}]}`, **siempre el mismo id**, un mensaje con adjunto que quedó a medias y que ni siquiera está en nuestra base. Baileys 6.7.24 corta ante CUALQUIER `stream:error` (`socket.js:507`) llamando a `getErrorCodeFromStreamError` (`generics.js:276`), que sin `code` en el nodo y sin `ack` en su tabla asume `badSession` (500): «tu sesión está corrupta». **La hipótesis era: quitando ese corte, la línea aguanta. Se desplegó, se midió, y NO aguanta.** Traza con el código nuevo ya corriendo: `22:57:48 Acuse ignorado` (el manejador propio NO cortó) → `22:57:48 connection errored` → `22:57:48 Conexión cerrada (428)` → `22:57:52 CONECTADO`. El 428 es `ws.on('close')` (`socket.js:445`): **WhatsApp cierra el socket él mismo**. El `stream:error` no era Baileys siendo paranoico — era WhatsApp avisando de que iba a terminar el flujo, y lo termina igual; Baileys solo llegaba primero. **La causa real:** el mensaje atascado vive en la cola de WhatsApp **para esa sesión concreta**. `linea_1` y `linea_2`, con el mismo código, llevan 2h15m sin una sola caída y sin recibir un solo acuse. **Se arregla re-vinculando esa línea** (desvincular y escanear el QR de nuevo), que le da sesión nueva y cola vacía. **No hay nada que tocar en el código.** El cambio se conserva y **el comentario se reescribió para que diga esto y no la hipótesis** —dejar una causa falsa dentro del código es lo que ya costó una arquitectura entera aquí (AGENTS.md §2.6)—: porque ahora el motivo del corte es verdad (428 «el otro lado cerró», en vez de un 500 «sesión corrupta» que manda a buscar el fallo donde no está), porque ese registro nuevo es lo que permitió encontrar la causa real, y porque no hace daño. **NO se actualizó Baileys**: la última publicada es 7.0.0-rc14, un candidato a versión, y un release candidate no sostiene las líneas de un negocio. | `wa-server-baileys/server.js` | **2h15m de observación** tras el reinicio de las 22:07:48. `linea_1` y `linea_2`: **cero caídas**, `connectedAt` intacto desde el arranque. `linea_3`: dos caídas, 22:57:48 y 23:47:52 — **50m04s otra vez**, las dos con el mismo id `3A0DF74A8F30B473DED8`, las dos con el manejador nuevo disparando («Acuse ignorado») y las dos cerradas igual por WhatsApp con 428. Reconexión en **4 segundos** cada vez. Contador `acuses_ignorados` visible en `/metrics`. | ⚠️ Desplegado y honesto: **no resuelve la caída**. Acción pendiente del dueño: re-vincular `linea_3` |
| 2026-08-02 | **El bot deja de delatarse hablando de «ejecutar la herramienta».** Captura del dueño: a un cliente le escribió *«…necesito ejecutar la herramienta de búsqueda»*. Rompe la regla de que por ningún motivo el cliente puede enterarse de que habla con un bot. El filtro de identidad (`IDENTITY_MARKERS`) tapaba **14** frases y las 14 hablaban de lo que el bot **ES** («soy un bot», «inteligencia artificial», «modelo de lenguaje»); **ninguna** de lo que el bot **HACE**. «Ejecutar la herramienta», «la base de datos», «el sistema no me deja» o «procesar tu consulta» pasaban enteras, y un asesor de verdad jamás diría eso. Se agregó la segunda familia, **siempre con contexto**, porque «herramienta» a secas es una categoría del catálogo de ZOOM: no se tapa la palabra suelta, se exige un verbo de ejecutar delante o «de búsqueda/consulta/precios» detrás. Igual con «sistema»: «el sistema de impresión DTF» es lenguaje de producto y no se toca; «el sistema no me deja» sí. Además se corrigió el efecto secundario: borrar la frase delatora dejaba **una disculpa sin ningún paso siguiente**, que es casi peor; ahora si lo que queda no da precio, no pregunta y no promete nada, se le añade el paso. **No se tocó el system-prompt** (regla del proyecto: cargarlo de instrucciones induce alucinaciones); esto se resuelve en el filtro determinista. | `lib/agent/index.ts` | **25/25** casos. Los 10 que debe tapar, incluida la frase exacta de la captura. Y los **15 falsos positivos que NO debe tapar**, que era lo que importaba: «Set de Herramientas 12 piezas», «kit de herramientas para el hogar», «el sistema de impresión DTF permite full color», «nuestro sistema de producción trabaja de lunes a viernes», «ya estamos procesando tu pedido», «la base del cuaderno es de 120 hojas». La frase de la captura, entera, pasa de terminar en «necesito ejecutar la herramienta de búsqueda» a terminar en «Dame un segundo y te confirmo la cotización exacta». Verificado que los marcadores nuevos están dentro del contenedor desplegado. Simulador sin regresión: banner laminado 200×100 = **$60.000**. | ✅ Desplegado y verificado |
| 2026-08-02 | **Ya se puede guardar un producto que se cobra por medida: el formulario habla el idioma de la unidad.** Palabras del dueño: «intento crear un aviso exterior y no lo puedo guardar, porque el panel solo guarda rangos de precio por volumen». **La base de datos NO tenía el problema y no se tocó**: `products.unit` ya acepta `m2`, `metro` y `millar`, hay 19 productos guardados con `m2`, y el bot los cotiza bien — el Banner Laminado (`ZM-GEN-279`) está a $30.000 el m² y para uno de 200×100 cm responde $60.000. Lo que fallaba eran **las palabras**: se elegía arriba «Unidad de medida: Metro Cuadrado (m²)» y abajo seguía diciendo «Rangos de Precios por Volumen», «Cantidad Mínima» y «Cantidad Máxima», sin ninguna pista de que para decir «$300.000 el metro cuadrado» hay que escribir desde 1, hasta vacío, precio 300000, unitario. Ahora la sección de precios se adapta a la unidad ya elegida: título, ayuda, rótulos de los campos, ejemplo de la variante (materiales y acabados cuando es por área: «Sin iluminación, Con LED interior, Backlight, Acrílico 5 mm»), y **una frase visible que dice exactamente qué escribir**. La primera fila de un producto por área viene con desde 1 / hasta vacío / «Precio por m²», y la ficha guardada muestra «$300.000 · por m²» en vez de «por unidad». Se agregó también metro lineal y millar. | `app/(app)/conocimiento/KnowledgeBaseClient.tsx` | Verificado en el contenedor desplegado que conviven **los textos nuevos** («Precio por metro cuadrado», «deja «hasta» vacío») **y los viejos** («Rangos de Precios por Volumen»): un producto por unidad sigue viendo exactamente lo de siempre, sin una palabra cambiada. Revisado el camino de guardado: `saveProduct` pasa `unit` tal cual y las únicas validaciones son nombre, categoría, variante no vacía, mínimo ≥ 1 y precio ≥ 0 — nada bloqueaba un producto por m². Build sin errores de tipos. | ✅ Desplegado; falta que el dueño guarde su primer aviso exterior |
| 2026-08-02 | **Notas de voz desde el panel (cierra el pendiente del 02-ago).** Quedaba en rojo porque para que un audio llegue como **nota de voz** —y no como un archivo que el cliente tiene que abrir aparte— WhatsApp exige `ogg/opus` marcado `ptt`, el navegador graba en `webm/opus`, y no había `ffmpeg` en ningún contenedor. Se resolvió en tres piezas: **(1)** `ffmpeg` entra en la imagen de la **app**, no del puente — así lo que queda guardado ya sirve para todos (el panel lo reproduce igual en Chrome que en un iPhone) y, si la conversión falla, se corta **antes** de que nada salga a WhatsApp; **(2)** el `send-media` del puente acepta la marca `ptt` y, si la pide, **exige** que el audio llegue en `audio/ogg` —mandar media nota de voz es peor que no mandarla—; **(3)** el micrófono en el cuadro de escribir, con el cambio micrófono ↔ flecha de WhatsApp: campo vacío = micrófono, algo escrito = flecha. Mientras graba se ve el tiempo y una papelera para descartar. Tope de 5 minutos: al llegar se corta sola y se envía, en vez de crecer hasta pasarse de los 20 MB. **El `ffmpeg` va en la última capa del Dockerfile a propósito**: puesto arriba invalidaría la capa del `npm install`, y sin `package-lock` eso deja que las librerías cambien solas. **Dato importante para el dueño: el micrófono solo funciona entrando por `https://zoompublicidad.tech`.** Los navegadores no dan micrófono en direcciones sin candado, así que en `http://2.25.169.103` no funcionaría nunca; el panel lo dice con un aviso claro en vez de quedarse mudo. | `Dockerfile`, `lib/whatsapp/nota-de-voz.ts` (nuevo), `lib/whatsapp/send.ts`, `lib/whatsapp/adapter.ts`, `app/api/conversations/send-media/route.ts`, `components/chat/message-input.tsx`, `wa-server-baileys/server.js` | Conversión probada **dentro del contenedor que corre**, con la misma tubería del código (`pipe:0` → `pipe:1`, sin archivos temporales): se fabricó lo mismo que graba Chrome en Windows (webm/opus, 3 s, 28.542 bytes) y salió **ogg/opus, 48 kHz, mono, 3,01 s, 14.730 bytes**. Decodificado entero: **cero errores**, y la cabecera es `OggS` + `OpusHead`, que es exactamente lo que WhatsApp espera. `ffmpeg 8.0.1` con `libopus` confirmado en la imagen. Verificado en el código de Baileys que `ptt === true` sobre un audio es lo que dispara la onda de nota de voz (`Utils/messages.js:133`). Build de app y puente sin errores; el puente compila (`node --check`). | ✅ Desplegado; falta que el dueño grabe una y la oiga en el teléfono |
| 2026-08-02 | **Ya se puede responder citando un mensaje desde el panel.** El panel sabía **ver** citas desde hace tiempo (el puente las extrae de lo que llega y la burbuja las pinta en su caja), pero no había forma de **crear** una: no se podía elegir a qué mensaje se le responde. Ahora cada burbuja tiene una flecha al costado y al tocarla aparece arriba del cuadro de escribir el recuadro con lo que se cita, con su «x» para quitarlo. **Es agregar, no modificar:** `sendWhatsAppMessage` **no se tocó** —carga la regla de «si no salió, no se guarda», que costó descubrir con mensajes fantasma— y la vía citada va al lado, en `sendWhatsAppReply`, con esa misma regla. En el puente, `send-text` acepta `quoted` y sin él hace exactamente el mismo envío de siempre. La respuesta enviada se guarda con la **misma forma** que un mensaje entrante citado, así que la burbuja la pinta sin ninguna rama nueva. La flecha no aparece en mensajes sin identificador de WhatsApp: mejor no ofrecer el botón que ofrecer uno que falla. | `lib/whatsapp/message-preview.ts`, `components/chat/message-bubble.tsx`, `app/(app)/conversaciones/[id]/client-page.tsx`, `lib/conversations/actions.ts`, `lib/whatsapp/send.ts`, `lib/whatsapp/adapter.ts`, `wa-server-baileys/server.js` | Verificado **contra el código de la propia librería** (`Utils/messages.js:488-506`) que la cita que arma el puente entrega exactamente los cuatro campos que Baileys lee: `quoted.key.fromMe`, `quoted.key.participant` con caída a `remoteJid`, `quoted.key.id` y `quoted.message`. Al responder a un mensaje propio, Baileys usa su propio `userJid` y no hace falta dárselo. Como `remoteJid` es el mismo chat, no se añade el campo de grupo. Build de app sin errores de tipos; el puente compila (`node --check`). | ✅ Desplegado; falta que el dueño responda citando y lo vea en el teléfono |
| 2026-08-02 | **Los stickers ya se ven en el panel, y no hacen hablar al bot.** El dueño mandaba stickers y en el panel no aparecía **ninguno, ni un rastro**: el puente los tiraba antes de reenviarlos, con el comentario «no aportan a una cotización y ensucian el hilo». Esa frase mezclaba dos cosas distintas: que no aporten a una cotización es cierto; que ensucien el hilo, no — **un cliente que manda un pulgar arriba ESTÁ contestando**, y al dueño le sirve verlo. Lo que de verdad había que evitar —que el bot le conteste a un dibujo— **se decide en la app, que es donde se decide si corre el agente**, no tirando el mensaje en el puente: descartarlo allí apagaba las dos cosas a la vez. Ahora el sticker se guarda y se ve, **no despierta al agente** (un dibujo no es una consulta, y podía disparar el candado de búsqueda fuera de tema) y **no se manda a describir con IA** (llega como `image/webp`, caía en la rama de las imágenes y gastaba una llamada por cada dibujo para acabar escribiendo «[Imagen adjunta de cliente: un pulgar amarillo]» debajo del propio sticker). | `wa-server-baileys/server.js`, `lib/whatsapp/webhook-processor.ts` | Cadena verificada de punta a punta: `classifyMedia` marca `kind: 'sticker'` (`server.js:1079`), viaja como `mediaType: 'sticker'` en el payload, y el adaptador de la app lo lee tal cual. El puente compila (`node --check`) y la app build sin errores. Desplegado en el reinicio del puente de las 22:07 UTC. | ✅ Desplegado; falta que el dueño mande un sticker y un GIF |
| 2026-08-02 | **Ya se pueden mandar fotos y archivos al cliente desde el panel.** Desde el panel solo se podía escribir texto: un asesor que quería mandar la foto de un producto, un arte para aprobar o una cotización en PDF tenía que salirse del sistema y hacerlo desde el celular — y ese mensaje ya no quedaba en la conversación del CRM. **La tubería ya existía entera**: el puente tiene `/messages/send-media` con el mismo freno por rechazos y la misma verificación del acuse que el texto, y el adaptador ya sabía llamarlo (es la vía por la que el bot manda las fotos de producto). Faltaba la función y el botón. Va por una **ruta con `FormData`** y no por una server action, porque esas admiten 1 MB de cuerpo y una foto de celular pesa más; subir el límite global habría tocado toda la app. Se guarda con la **misma forma que un adjunto entrante**, así que la burbuja lo pinta sin ninguna rama especial. **`sendWhatsAppMessage` no se tocó**: la de media va al lado. Y rige la misma regla de siempre: **si no salió, no se guarda**. | `lib/whatsapp/send.ts`, `app/api/conversations/send-media/route.ts` (nuevo), `components/chat/message-input.tsx` | Build sin errores. La ruta responde **401 sin sesión** (existe y está protegida), y además comprueba que la conversación sea de la organización de quien la pide, porque la clave de servicio salta las políticas de la base. Verificado que el botón y la función están dentro del contenedor que corre. **Falta la prueba en pantalla del dueño.** | ⚠️ Desplegado, pendiente de prueba del dueño |
| 2026-08-02 | **La nota de voz desde el panel queda pendiente, y se explica por qué.** Para que un audio llegue como **nota de voz** —y no como un archivo de audio que el cliente tiene que abrir aparte— WhatsApp exige `ogg/opus` marcado como `ptt`. El navegador de Windows graba en `webm/opus`, que es otro envase, y **el contenedor del puente no tiene `ffmpeg`** (verificado: `which ffmpeg` no devuelve nada), que es lo único que convierte de uno a otro. Además el `send-media` del puente no acepta hoy la marca `ptt`. Se decidió **no entregarlo a medias**: un micrófono que manda un archivo que el cliente no puede oír de un toque es peor que no tener micrófono. | — | Comprobado en el contenedor del puente: sin `ffmpeg`. Comprobado en `server.js`: `ptt` solo se lee al RECIBIR, nunca al enviar. | 🔴 Pendiente: requiere `ffmpeg` en la imagen del puente y la marca `ptt` en `send-media` |
| 2026-08-02 | **Los videos y los GIF ya se ven en el panel; los stickers, en su tamaño.** El puente descarga y guarda los videos desde siempre (`classifyMedia` los reconoce), pero el panel solo sabía pintar **dos** cosas: imagen y audio. Un video o un GIF se bajaban, ocupaban su espacio en el almacenamiento… y en pantalla no aparecía nada. Un GIF de WhatsApp **no viaja como .gif**: viaja como un mp4 corto, así que los dos caen en la misma rama y se pintan con el mismo reproductor. Los stickers sí se veían —llegan como `image/webp` y caían en «es imagen»— pero a ancho completo, como si fueran una foto; ahora van en 112 px. Decisión del dueño: **solo ver, no enviar** — no se agrega selector de emojis ni nada para mandarlos. | `lib/whatsapp/message-preview.ts`, `components/chat/message-bubble.tsx` | **7/7** casos del clasificador (foto, sticker, GIF, video, nota de voz, foto de producto y texto pelado). Y la prueba que importaba, contra los datos que ya existen: **los 7 adjuntos reales de la base —4 notas de voz y 3 imágenes— clasifican exactamente igual que antes**, ninguno cambió de rama. Build sin errores; el panel levanta sin errores en el registro. | ✅ Desplegado; falta que el dueño mande un GIF y un sticker para verlo en pantalla |
| 2026-08-02 | **El interruptor del bot y la columna del Kanban dejan de contradecirse.** El dueño lo vio en una captura: la campana decía **3 conversaciones «piden hablar con una persona»** y la columna «Sin Atender» tenía **2**. La tercera seguía en «Entrada» con el bot apagado. No era un fallo de la campana ni del Kanban: **cada uno miraba un dato distinto para responder la misma pregunta** —¿esta conversación necesita una persona?—. La campana mira `bot_active`; el Kanban mira la etapa. Mover la tarjeta ya apagaba o encendía el bot (`updateContactStageAction`), pero al revés no: apagar el bot a mano desde el chat dejaba la tarjeta donde estaba. Ahora `toggleBotAction` ata las dos: apagar el bot manda la tarjeta a «Sin Atender» y encenderlo la devuelve a «Entrada». Las etapas puestas por una persona con otra intención no se tocan: «Molesto» al apagar (ya está donde debe) e «Ignorar» al encender (es un chat interno). | `lib/conversations/actions.ts` | Medido en base antes del arreglo: **1 conversación descuadrada** (`573205950326@s.whatsapp.net`, en «Entrada» con el bot apagado) — exactamente la que se veía en la captura. Corregida esa fila, campana y Kanban cuadran: **3 piden una persona y 3 en «Sin Atender»**. Build sin errores. | ✅ Desplegado y verificado |
| 2026-08-02 | **Si el bot no avanza dos turnos seguidos, la conversación pasa sola a «Sin Atender».** El dueño mostró una conversación de su panel: el otro lado era **otro bot** —de prospección, ofreciendo páginas web— y el de ZOOM le contestó **dos veces seguidas el mismo saludo** antes de cotizarle portarretratos. Nadie se enteró: quedó en «Entrada», con el bot encendido, dando vueltas. Hasta hoy una conversación solo llegaba a «Sin Atender» si el cliente pedía una persona o si el modelo decidía escalar; **nada detectaba que el bot se hubiera trabado**, que es el caso más común y el más silencioso. Dos señales, las dos duras: el bot entregó una de sus **frases de respaldo** (el propio código admitiendo que agotó los reintentos) o **repitió lo que ya había dicho**. Umbral elegido por el dueño: **a la segunda vez seguida** — un tropiezo suelto lo remonta solo, dos ya no. **Sin contadores en la base**: se deduce de los mensajes anteriores del bot, que ya vienen cargados en el turno; un contador guardado puede desincronizarse, los mensajes no. Los textos de respaldo se movieron a `request-human-handoff.ts` para que **la frase que se entrega y la que detecta el fallo sean literalmente la misma cadena** — si se reescribe una y la copia queda vieja, el sistema deja de darse cuenta de que está fallando. Va **antes** de mover la tarjeta: así la etapa queda en «Sin Atender» y `moverEtapaSiAvanza` se niega a tocarla; al revés, un turno trabado que igual soltó un precio se habría ido a «Ventas». | `lib/agent/tools/request-human-handoff.ts`, `lib/agent/index.ts` | **9/9** casos de la regla: aguanta la primera repetición y el primer respaldo, traspasa a la segunda seguida, y **no traspasa si el bot se recupera**. La batería del Kanban sigue en **11/12**, sin regresiones. **Límite medido y dicho de frente: la conversación que originó esto NO se habría marcado** — el bot repitió el saludo dos veces y siguió; con este umbral hace falta una tercera. | ✅ Desplegado y verificado |
| 2026-08-02 | **El cliente nunca se entera de que habla con un bot.** El respaldo del traspaso decía *«Te paso con un humano en un momento»*: una frase que solo tiene sentido si quien la dice NO es humano, y que salía justo en el momento más delicado, cuando el cliente ya pidió hablar con alguien. Estaba **dormida** —solo salta con `handoff_message` vacío en el panel, y hoy está lleno—, esperando a que alguien borrara ese campo. El respaldo no puede omitirse (hay que contestarle algo al cliente) pero sí puede no decir nada: ni que es un bot, ni un dato del negocio que nadie escribió en el panel. Regla del dueño (02-ago): **por ningún motivo un cliente puede enterarse de que habla con un bot.** | `lib/agent/tools/request-human-handoff.ts` | Auditoría de **todo** el texto fijo que puede llegarle al cliente desde `index.ts`, `request-human-handoff.ts` y `system-prompt.ts`: era la única frase que lo delataba. Las demás coincidencias eran registros internos y descripciones de herramientas, que el cliente no ve nunca. | ✅ Desplegado |
| 2026-08-02 | **Al cliente que pide una persona se le da una persona, y se deja de depender de que el modelo se acuerde.** Probando el Kanban con el simulador salió el agujero: ante «No me sirve el bot, quiero hablar con un asesor humano de verdad por favor», el bot contestó **«Jaja, soy Oscar, del equipo comercial. Cuéntame qué necesitas.»** y no traspasó nada — se quedó en «Entrada», con el bot encendido y sin avisarle a nadie. **A un cliente que pidió una persona se le negó la persona.** Misma causa que vaciaba el Kanban: el traspaso vivía en `requestHumanHandoff`, una herramienta que el modelo llama si se le ocurre. Ahora se decide en código **antes de que el modelo abra la boca**: si el mensaje pide hablar con alguien, se traspasa, se apaga el bot, se mueve a «Sin Atender» y se responde con la frase de traspaso del panel. La herramienta se conserva para lo que la regla no puede ver (una queja grave, un caso irresoluble) y **el cuerpo del traspaso se extrajo a `ejecutarTraspasoAHumano`**, así las dos vías apagan el bot, mueven la etapa y avisan al equipo exactamente igual. | `lib/agent/tools/request-human-handoff.ts`, `lib/agent/index.ts` | Batería de 12 comprobaciones por el simulador, sin enviar un solo WhatsApp. **Antes 9/12, después 11/12.** La detección: **18/18** casos dirigidos y **0 falsos positivos** sobre las 51 frases guardadas de la tanda de prueba — importa porque esto APAGA el bot, y «necesito 100 mugs para una persona» correctamente no dispara. Verificado además que el código **no pisa una etapa puesta por una persona**: un contacto en «Molesto» que pide precio sigue en «Molesto» con el bot apagado. Y la campana ya tiene qué mostrar: 4 avisos (1 molesto, 3 listos para pagar) donde antes no había nada, porque las columnas estaban vacías. | ✅ Desplegado y verificado en producción |
| 2026-08-02 | **La tarjeta del Kanban se mueve sola, con lo que pasó: el Pipeline deja de depender de que al modelo se le ocurra.** Mover una tarjeta era trabajo del modelo —existía `updatePipelineStage` y él decidía cuándo llamarla—, que es pedirle a un asesor que además de atender se acuerde de arrastrar la ficha. **Medido sobre 32 conversaciones: el modelo movió 2 tarjetas mientras 18 clientes ya habían recibido una cotización con precio; 17 de ellos seguían en «Entrada».** El Kanban mostraba Ventas vacía con 18 cotizaciones sobre la mesa, así que no servía para trabajar. No se arregla insistiéndole en el prompt: es la clase entera de fallo — **cualquier dato del CRM que dependa de que el modelo se acuerde de escribirlo va a estar mal la mayoría de las veces**—, y además cargar reglas al prompt induce alucinaciones (regla del proyecto). Ahora la etapa se deduce de los hechos del turno: **«Ventas»** cuando el bot entregó una cotización con precio —hecho duro, porque el candado antialucinación ya garantiza que un precio en la respuesta salió de la calculadora—, y **«Listo para pagar»** cuando el cliente escribe que compra o pregunta dónde pagar. Decisión del dueño (02-ago): esa columna la mueven **las dos vías**, la automática y la herramienta del modelo, la primera que se dé cuenta. **La escritura es una sola** (`moverEtapaSiAvanza`), y por ahí pasan las dos: solo se avanza, y una etapa puesta por una persona (`angry`, `unhandled`, `ignore`) no se toca jamás, porque volvería a encender un bot que alguien apagó a propósito. | `lib/agent/pipeline-automatico.ts` (nuevo), `lib/agent/index.ts`, `lib/agent/tools/update-pipeline-stage.ts` | **16/16** casos dirigidos de la señal de cierre, incluidas las negaciones que la dan vuelta («no lo tomo», «todavía no sé cómo pago», «aún no lo compro»). Contra las **51 frases de cliente guardadas de la tanda de prueba** dispara **una sola vez**, en «Listo, lo tomo. ¿A dónde consigno?»: cero falsos positivos, con «¿me haces mejor precio?» y «estamos comparando dos proveedores» correctamente quietas. La prueba destapó que «mándame los datos de pago» **con tilde** no se detectaba; corregido. De extremo a extremo por el simulador, contacto nuevo: pide precio → el bot cotiza → la tarjeta pasa **sola a «Ventas»**; dice «Listo, lo tomo. ¿A dónde pago?» → pasa **sola a «Listo para pagar»**. Build sin errores. | ✅ Desplegado y verificado en producción |
| 2026-08-02 | **Un canal de WhatsApp no es un cliente: solo las personas entran al CRM.** Entró `120363315571514607@newsletter` y el sistema le abrió **ficha sin nombre y conversación** en `linea_3` (1 mensaje, 0 respuestas): en el panel se veía como un cliente más y no había forma de saber que no lo era. **La causa no era el newsletter.** El filtro del puente era una **lista negra** —grupos, difusiones y estados— y todo lo demás pasaba; WhatsApp estrena tipos de chat cada tanto (canales, comunidades, chats de IA) y cada uno se colaría igual, de a uno, hasta que alguien lo notara. Tapar `@newsletter` habría sido corregir el síntoma. **Se invierte la regla: pasa lo que SÍ es una persona** (`@s.whatsapp.net`, `@c.us`, `@lid` y dígitos sueltos), y nada más. La compuerta va en **dos sitios con oficios distintos**: en `contact-identity`/`webhook-processor`, que es **donde nace la ficha** y adonde llegan TODOS los puentes —así el invariante se sostiene aunque mañana vuelva otro puente—, y en el puente, para no gastar el viaje ni la corrida del agente. Invertir a lista blanca puede dejar fuera **en silencio** a un cliente real si WhatsApp estrena un dominio nuevo de persona —ese error ya se cometió aquí enumerando las líneas una por una—, así que **el descarte no es mudo**: se cuenta por dominio en `/diagnostic` (`chatsNoPersona`) y cada chat nuevo se anota una vez en el log. | `lib/whatsapp/contact-identity.ts`, `lib/whatsapp/webhook-processor.ts`, `wa-server-baileys/server.js` | **13/13** casos de la regla: pasan los 4 formatos de cliente real, quedan fuera canal, grupo, difusión y estados. **La regla aplicada a los 32 contactos que ya existían: pasan los 30 clientes, quedan fuera exactamente los 2 canales** (los dos sin nombre; los 30 con nombre). Contra el webhook real, un mensaje de canal: contactos **32 antes y 32 después**, con el log «Chat que no es una persona» y `dominio: newsletter`. `/diagnostic` expone `chatsNoPersona`. Mientras se trabajaba **entró un segundo canal a las 13:26** y creó ficha — el fallo estaba vivo en producción. Build de app y puente sin errores. | ✅ Desplegado y verificado en producción |
| 2026-08-02 | **Prueba masiva: 30 clientes distintos contra el bot real, sin gastar un mensaje de WhatsApp.** Se le abrió al simulador que ya existía en Personalización (`POST /api/agent/test`) una segunda puerta autenticada con `x-bridge-key` —la MISMA llave que ya usa el webhook, así que no se abrió superficie nueva— para poder crear contacto y conversación y correr tandas sin que una persona las escriba a mano. Se borró todo lo anterior y se corrieron **30 conversaciones con 30 contactos propios repartidos entre las 3 líneas**, cubriendo saludo, datos del negocio, catálogo y cotización, cambio de tema a mitad, objeciones, negociación, cierre y pedido de foto. **Resultado: 31 conversaciones, 99 mensajes, 49 respuestas aprobadas por el candado, 0 respuestas de respaldo.** Lo que funcionó: saludo e identidad, precios reales del catálogo, cambio de tema sin arrastrar el producto anterior, objeciones, cierre a «Vendido», y la única vez que dijo «lo consulto con el equipo» fue por un dato del negocio **que está vacío en el panel** — comportamiento correcto, no un fallo. **Lo que NO funcionó, pendiente de corregir:** (1) **el pipeline casi no se usa** — de 3 negociaciones activas solo 1 pasó a «Ventas»; «¿me haces mejor precio por esa cantidad?» y «estamos comparando dos proveedores» se quedaron en Entrada; (2) **un newsletter de WhatsApp creó una conversación** (`120363315571514607@newsletter`, 1 mensaje, 0 respuestas): los canales y newsletters no deberían generar chats. | `app/api/agent/test/route.ts` | La tanda es la prueba. Evidencia viva en el panel: 10 conversaciones en `linea_1`, 10 en `linea_2`, 11 en `linea_3`; etapas `inbox: 29`, `sales: 1`, `sold: 1`. | ⚠️ Simulador desplegado y tanda ejecutada; **2 fallos abiertos** (pipeline y newsletters) |
| 2026-08-02 | **Una línea propia no es un cliente: se corta el bucle entre bots.** Un mensaje entre dos líneas del mismo negocio hacía que el agente de cada lado contestara al otro **sin fin**. Medido: **14 mensajes en 2 minutos** entre la línea 1 y la línea 2, y no paraba solo — lo detuvo el dueño avisando. Con las 8 líneas previstas esto no es un caso raro: basta que alguien escriba de un local a otro para quemar dos números **reales** a base de mensajes automáticos, que es exactamente lo que gatilla los bloqueos de WhatsApp. El puente sabe qué números son suyos —son las líneas que él mismo atiende—, así que ahora un mensaje entrante desde otra línea propia se registra y **no se reenvía al agente**. Resuelve también cuando el remitente llega como `@lid`, usando el teléfono aprendido. | `wa-server-baileys/server.js` | Con el bot **activo** en las 7 conversaciones, envío `linea_1 → linea_2`: el puente registró «Mensaje recibido desde OTRA LÍNEA PROPIA» y el agente respondió **cero veces**. Antes del arreglo, ese mismo envío disparaba el ida y vuelta indefinido. | ✅ Desplegado y verificado en producción |
| 2026-08-02 | **La restricción de WhatsApp es de la CUENTA, no de la ranura — y por atarla a la ranura el puente bloqueó un número sano.** El día anterior la restricción se guardó dentro de la carpeta de la ranura y **sin anotar de qué número era**. El dueño conectó un número **nuevo** a la ranura 2 y el puente le negó **todos** los envíos aplicándole el castigo del número anterior. En los registros **no hay un solo 463 del número nuevo**: los únicos rechazos eran las negativas del propio guardián. El dueño lo detectó porque desde WhatsApp Web ese número escribía a números nuevos sin problema — tenía razón, y la insistencia evitó que el fallo quedara enterrado bajo una explicación cómoda. Ahora la restricción se anota **con su teléfono** y solo aplica a ese número; si la ranura cambia de número, se descarta y se borra el archivo. Es la misma clase de error que agrupar conversaciones por ranura: **los datos de una cuenta nunca se atan a la ranura**. | `wa-server-baileys/server.js` | Antes: 7 negativas seguidas del guardián al número nuevo, cero rechazos reales de WhatsApp. Después: `/diagnostic` muestra `restriccionWhatsApp: null` en ambas líneas, y el envío de prueba **línea 2 (número nuevo) → línea 1 entregó con HTTP 200**. | ✅ Desplegado y verificado en producción |
| 2026-08-02 | **Se descubre de dónde sale el 463, y el sistema deja de esconderlo.** La línea 2 no entregaba: el panel decía «conectada», el código era el mismo de la línea 1, y cada envío moría con un 463 sin explicación. El dueño insistió —con razón— en que no podía ser WhatsApp, porque el mismo número enviaba bien desde el celular. **El dato estaba llegando desde el principio y se tiraba a la basura**: WhatsApp le había mandado a esa cuenta una notificación que Baileys descarta como *«Invalid mex newsletter notification»*. Decodificada: `xwa2_notify_account_reachout_timelock` con `enforcement_type: RESTRICT_ALL_COMPANIONS`, `is_active: true`, `time_enforcement_ends: 1786218427` (sábado 08-ago 14:47, siete días justos desde que WhatsApp cerró la sesión). **`RESTRICT_ALL_COMPANIONS` restringe todos los dispositivos vinculados —Baileys, WhatsApp Web, Desktop— pero NO el teléfono**, y de ahí la confusión: desde el celular se envía bien mientras el puente recibe 463. Ahora el puente lee esa notificación, **la guarda en disco** (WhatsApp la anuncia una sola vez y un reinicio borraba la única explicación), la expone en `/diagnostic` como `restriccionWhatsApp` y responde a los envíos con la fecha de fin. | `wa-server-baileys/server.js` | Prueba cruzada con los dos números del dueño, mismo puente y mismo minuto: **línea 2 → línea 1 = 463**, **línea 1 → línea 2 = entregado**. Descartadas ranura (no hay una sola línea de código específica para `linea_2`), `@lid` (el envío que falló fue a un teléfono normal) y destinatario. Con la restricción cargada, el envío pasa de morir con 463 a responder `503` con el texto y la fecha exacta; `/diagnostic` la muestra en la línea 2 y `null` en la 1. | ✅ Desplegado y verificado en producción |
| 2026-08-02 | **Freno automático cuando WhatsApp rechaza envíos: el sistema deja de insistir.** Un acuse `463` es WhatsApp diciendo *«pará»*, y el puente no distinguía entre «no salió por la red» y «me están frenando»: seguía respondiendo a cada mensaje entrante. Medido en la línea 2 el 01-ago: **de 13 intentos de envío, 10 rechazados en 51 minutos**. Ninguno llegó al cliente y cada uno hundía más la cuenta — así es como un bloqueo temporal se vuelve permanente, y era el riesgo real de conectar las 8 líneas de la empresa. Ahora, tras **3 rechazos seguidos** la línea deja de **enviar** durante un tiempo que crece solo (10 min → 30 min → 2 h → 6 h); un envío entregado limpia el contador y levanta el castigo. **Recibir no se toca**: ninguna consulta se pierde, quedan en el panel para atenderlas cuando la línea se recupere o desde otra línea. El `/diagnostic` expone `enReposo`, `reposoMinutosRestantes`, `reposoHasta` y `rechazosSeguidos` para que el panel lo muestre. | `wa-server-baileys/server.js` | `node --check` OK; puente desplegado y `linea_1` reconectada; `/diagnostic` devuelve los cuatro campos nuevos. **Pendiente:** ver el freno dispararse de verdad requiere una línea castigada; la línea 2 quedó en reposo por decisión del dueño. | ✅ Desplegado; disparo real pendiente de observación |
| 2026-08-02 | **Una sesión revocada por WhatsApp ya se puede recuperar desde el panel.** Cuando WhatsApp cierra una línea (`conflict: device_removed`), las credenciales quedan revocadas. El puente lo detectaba y escribía «hay que borrar la sesión», **pero no la borraba** — eso solo lo hacía el botón «Desvincular número». Y dejarlas puestas no es inofensivo: **un QR solo se emite cuando NO hay credenciales**, así que cada intento de vincular volvía a `logging in…` con el usuario viejo, WhatsApp respondía `Connection Failure`, y la línea quedaba en «Generando QR…» para siempre. Medido el 01-ago: la línea 2 quedó irrecuperable desde el panel; hacía falta entrar al servidor a borrar la carpeta. Con 8 puntos de venta eso es un local caído hasta que aparezca un técnico. Ahora las credenciales revocadas se borran solas y la línea queda lista para un QR nuevo. | `wa-server-baileys/server.js` | Tras desplegar, el registro pasó de `logging in…` + `Connection Failure` a **«credenciales borradas, la línea queda lista para un QR nuevo»**, y el siguiente pedido de QR devolvió `not logged in, attempting registration` con la imagen del QR. El dueño reconectó la línea 2 desde el panel. | ✅ Desplegado y verificado en producción |
| 2026-08-01 | **Un mensaje escrito por un asesor desde el panel ya no se muestra como enviado si no salió.** `sendWhatsAppMessage()` insertaba el mensaje en el chat **pasara lo que pasara** y devolvía `true` siempre, incluso cuando el puente no había podido entregarlo (`sendTextMessage` → `null`). El asesor veía su mensaje en el panel, daba por hecho que el cliente lo había recibido, y se quedaba esperando una respuesta que nunca iba a llegar. Es el mismo fallo de «respuesta fantasma» que la ruta del BOT ya tenía resuelto desde el 31-jul en `webhook-processor` — la ruta del panel nunca se corrigió, y un mensaje fantasma escrito por una persona es peor, porque nadie sospecha del sistema. Ahora sin acuse de WhatsApp no se guarda nada, la acción devuelve error para que el panel lo muestre, y el fallo queda registrado en `line_error_log` con su causa. | `lib/whatsapp/send.ts` | Medido en la base antes del arreglo: de los **6** últimos mensajes escritos a mano, **3 no tenían id de WhatsApp** — nunca salieron. Los tres eran de `linea_2` (19:58, 20:51 y 21:08), que estaba caída desde las 14:47 por `device_removed`. Build sin errores de tipos. La comprobación en pantalla la hace el dueño: ahora debe salir un error en vez de un mensaje aparentemente enviado. | ⚠️ Desplegado; pendiente de confirmación del dueño en pantalla |
| 2026-08-01 | **Una línea que WhatsApp cierra vuelve a dar QR sola; antes quedaba muerta hasta que alguien entrara al servidor.** A las 14:47 (hora Colombia) WhatsApp cerró `linea_2` con `stream:error 401 / conflict: device_removed` — la desvincularon desde el teléfono. El puente lo detectaba bien y escribía *«hay que borrar la sesión y escanear un QR nuevo»*… **pero no la borraba**: eso solo lo hacía el botón «Desvincular número». Y dejar las credenciales revocadas en disco no es inofensivo, porque **un QR solo se emite cuando NO hay credenciales**: cada intento de vincular volvía a `logging in…` con el usuario viejo, WhatsApp respondía `Connection Failure`, y el panel se quedaba en «Generando QR…» para siempre. Con 8 puntos de venta, eso es un local sin atención hasta que aparezca un técnico. Ahora, ante una sesión revocada, el puente **borra las credenciales en el acto** y limpia el número y el QR viejos del estado, para que el panel no siga mostrando el teléfono anterior. Es la misma reconexión para las 8 líneas: no hay código especial por línea. | `wa-server-baileys/server.js` | Reproducido en producción: tras desplegar, el registro pasó de `logging in… (username: 573160598645)` → `Connection Failure` a **«Sesión revocada por WhatsApp: credenciales borradas»** y después **«not logged in, attempting registration…» → «QR disponible para linea_2»**, con el QR devuelto de verdad por el endpoint del panel. `linea_1` reconectó sola tras el redespliegue, sin errores. Verificado también que la caída **no** fue por el cambio del `@lid`: `linea_1` envió a un `@lid` sin teléfono a las 12:48 y siguió conectada; el `device_removed` es el único de todo el historial. | ✅ Desplegado y verificado en producción |
| 2026-08-01 | **El modo de cobro sale de la configuración del producto, no de palabras de su nombre.** `getProductPrice` elegía cómo cotizar buscando cadenas en el nombre (`DTF TEXTIL`, `VINILO`, `BANNER`, `TARJETA`…), y el `select` de ese producto **ni siquiera traía la columna `unit`**: `unit` valía siempre `''`, así que la Unidad de medida que el dueño elige en el panel no se consultaba nunca. Consecuencia con un cliente real: la **Gorra Dril 5 Paneles DTF Textil** (`ZM-TEX-031`) está perfectamente configurada —`unidad`, mínimo 6 y seis rangos por cantidad de $14.000 a $10.500— pero su nombre contiene «DTF Textil», así que caía en la regla del **rollo de transferencia**: el bot pidió ancho y alto del logo, el cliente insistió y los dio (5×5 cm), y la cotización terminó en error. Nunca miró los seis precios que tenía al lado; la venta se perdió pidiendo un dato que no cambiaba nada. Lo mismo rompía `Araña 2x1m con Impresión Banner` y `Roll Up 2x1m con Impresión Banner` ($110.000 y $130.000 la unidad), secuestrados por la palabra «BANNER». Arreglo: se carga `unit` y **un producto con unidad `unidad` se cotiza por cantidad y punto**; las reglas por área quedan para lo que de verdad se mide. En `calculateCustomPrice`, si no hay `pricing_rules` de la categoría y el producto va por unidad, se devuelve al modelo a `getProductPrice` con la orden explícita de **no pedir medidas**. | `lib/agent/tools/get-product-price.ts`, `lib/agent/tools/calculate-custom-price.ts` | Reproducida la conversación que falló. «una docena de gorras dril 5 paneles DTF textil» → **$13.000 c/u, $156.000 por 12**, la casilla exacta de la tabla del dueño, sin pedir una sola medida. «araña con impresión en banner» → las 3 opciones con su precio unitario correcto. No regresiones en lo que sí va por área: «banner laminado de 200 x 100 cm» → 2 m² × $30.000 = **$60.000**. Tarjetas y volantes no se tocan (`unit = millar`). | ✅ Desplegado y verificado en producción |
| 2026-08-01 | **Renombrar la línea desde la pantalla donde se administra.** El nombre visible ya existía (`whatsapp_lines.display_name`), ya se mostraba en cada chat («Escribió a …») y en la cabecera de la conversación, y ya tenía su `PATCH`. Pero el único sitio para cambiarlo era «Líneas de WhatsApp», y quien administra entra por **Integraciones → WhatsApp**, donde solo veía «Línea 1, Línea 2» y daba por hecho que no se podía tocar. Se agregó el lápiz de renombrar en la tarjeta de cada línea. Cambia **solo** el nombre visible: `line_key` (`linea_1`, `linea_2`…) es la clave de enrutado y de agrupación de conversaciones, y no se toca ni se muestra. Con los 8 puntos de venta previstos, ese nombre es lo único que le dice al asesor de qué local le escriben. | `app/(app)/integraciones/whatsapp/client-page.tsx` | Build sin errores de tipos; la pantalla responde y el `PATCH` es el mismo que ya usaba «Líneas de WhatsApp». | ⚠️ Desplegado, pendiente de prueba del dueño |
| 2026-08-01 | **Se le responde a quien escribe sin número, y se deja de fabricar chats fantasma.** Tres fallos que salieron de una conversación real. **(1) El puente se negaba a entregar.** Un contacto escribió tres veces y el bot no contestó; el agente **sí** había redactado y aprobado las tres respuestas. Quien las frenó fue `resolveSendJid()`, que rechazaba con 503 todo `@lid` sin teléfono conocido «porque WhatsApp lo rechaza con 463» — la causa falsa del 30-jul que **este mismo repositorio desmintió el 31-jul**: el 463 es bloqueo de CUENTA, no de dirección. Se corrigió el documento pero nunca el código. Y el teléfono no iba a llegar: los tres mensajes trajeron `senderPn: null`, `senderLid: null`, `participantPn: null` (privacidad nueva de WhatsApp, se escribe sin exponer el número), así que no había nada que aprender. Ahora **se intenta el envío y decide el acuse REAL**, que ya se verificaba con `esperarRechazo()`. **(2) Personalización creaba una conversación por visita.** Buscaba la del simulador con `.single()`, que falla con más de una fila; el código concluía que no existía y creaba otra. **10 conversaciones del contacto de pruebas, 9 sin un solo mensaje**, todas arriba en la bandeja porque la lista ordena por fecha del último mensaje y una recién creada trae la de hoy — de ahí la sensación de que «los chats de ayer estaban por encima de los de hoy». Ahora toma la más antigua. **(3)** El chat del simulador (`+10000000000`) deja de mezclarse con clientes reales en la bandeja; se sigue probando desde Personalización. | `wa-server-baileys/server.js`, `app/(app)/personalizacion/page.tsx`, `lib/conversations/list.ts` | Registros del puente y de la app de la conversación real (`e3724a23`): 3 × «Agent finished» + 3 × rechazo del puente, con `senderPn: null` en las tres entradas. Conteo en base: 27 conversaciones, 9 vacías, **las 9 del simulador** — verificadas una a una antes de borrar. Build sin errores de tipos; `node --check` del puente OK; los tres servicios arriba y `linea_1` reconectada tras el redespliegue. **Pendiente:** la entrega a un `@lid` sin teléfono la confirma el dueño escribiendo desde esa cuenta (no se envía a terceros desde aquí). | ✅ Desplegado (`ba6f351`); limpieza hecha; entrega al `@lid` pendiente de confirmación del dueño |
| 2026-08-01 | **El bot deja de ofrecer lo de antes cuando el cliente cambia de tema.** El rail de recuperación pegaba las palabras del mensaje anterior a la consulta nueva. Medido en producción: ante *«mejor dime que valen unos pocillos»* buscaba `unos pocillos cuadernos` y devolvía **12 cuadernos y cero pocillos**, que se le entregaban al modelo bajo el rótulo «productos que coinciden con lo que pidió el cliente»; con *«y unos esferos que valen?»*, 4 cuadernos y cero esferos. Con **avisos** —que no está en el catálogo— ese arrastre era lo único que quedaba y el bot cotizaba los mugs del mensaje anterior (registro del 31-jul: el cliente escribe «avisos» y el rastro dice `query: "mug"`). Tres cambios: (1) el historial solo se arrastra si el mensaje **no nombra nada por sí mismo**, así *«las de 20por30»* sigue heredando el hilo pero «avisos» ya no hereda «mug»; (2) candado nuevo **`search-off-topic`** — si el modelo buscó en el catálogo y **ninguna** de sus consultas tiene que ver con lo que el cliente acaba de escribir, se bloquea la respuesta y se le ordena buscar eso o preguntar; va **primero** en el orden de los candados porque es la causa y los demás solo ven síntomas; (3) preguntar deja de penalizarse en ese caso y el respaldo pregunta en vez de prometer una cotización. De paso, `extractCitedReferences` leía la palabra «Referencia» como la referencia inventada `ERENCIA` y quemaba los reintentos. | `lib/agent/index.ts` | 8 mensajes por el webhook real (`linea_1` contra su propio número, sin tocar clientes). **Antes:** «avisos» → cotización de mugs. **Después:** «avisos» → el candado dispara (`pidio: avisos,local`), el modelo rebusca con «avisos para local» y el bot **pregunta** qué necesita. No regresiones: «cotizame unos pocillos» → 3 mugs con precio (aprobado al 1er intento); «el sencillo de 11oz pero para 100» → cotiza el mug correcto; «bolsas de organza» → las 3 de organza; **«las de 20por30, unas 100»** → Bolsa Organza 20x30 cm, aprobada al 1er intento. Build sin errores de tipos. | ✅ Desplegado y verificado en producción (`58d572b`) |
| 2026-08-01 | **Se ve a qué línea escribe el cliente, y las líneas se pueden renombrar.** En la lista de chats la línea era un punto de 6 px de color: con dos se toleraba, con las 8 previstas nadie recuerda que el cian es la línea 4. Ahora va el **nombre escrito** con su color, y la **cabecera del chat** muestra `cliente → Línea · número`, que es el dato que faltaba: un asesor podía responder sin saber desde cuál de sus números salía. Además el nombre visible **ya se puede cambiar** (`PATCH /api/whatsapp-lines/[line_key]`, enlace «Renombrar» en el panel): antes solo se fijaba al crear la línea, así que todas quedaban como «Línea 1», «Línea 2». `line_key` no se toca — es la clave de enrutado y de agrupación de conversaciones. | `components/chat/conversation-list.tsx`, `app/(app)/conversaciones/[id]/client-page.tsx`, `app/api/whatsapp-lines/[line_key]/route.ts`, `app/(app)/lineas/lineas-client.tsx` | `PATCH` sin sesión → rechazado y nombres intactos en la base. Build sin errores de tipos. | ⚠️ Desplegado, pendiente de prueba del dueño |
| 2026-08-01 | **«Vendido» era mentira: pasa a «Listo para pagar».** El bot movía ahí al cliente cuando **decía** *«lo tomo»* — eso es una intención, no una venta. Y nadie lo sacaba nunca: un cliente que volvía semanas después seguía marcado, y con la campana nueva habría figurado como pendiente de cobro para siempre. Ahora **sale solo**: si vuelve a pedir cotización regresa a «Ventas». Se descartó detectar el pago automáticamente leyendo comprobantes —decisión del dueño, con razón: se falsifican, y el peor caso es dar por despachado lo que nadie despachó—. Queda una columna **«Vendido» visible pero bloqueada**, rotulada como módulo por activar: una columna vacía sin explicación se lee como algo roto o invita a ensuciar los datos. | `lib/agent/tools/update-pipeline-stage.ts`, `lib/whatsapp/webhook-processor.ts`, `app/(app)/kanban/client-page.tsx` | Comprobado que nada devolvía el contacto a «Ventas»: la etapa solo cambiaba por acción humana o por el propio bot. | ⚠️ Desplegado, pendiente de prueba del dueño |
| 2026-08-01 | **La campana vigilaba un interruptor, no una situación.** Preguntaba solo `bot_active = false`, así que las líneas de procesos internos —con el bot apagado a propósito— notificaban eternamente, y una campana siempre encendida deja de mirarse. Ahora vigila **quién necesita a una persona**: molestos (con bot o sin bot), listos para pagar, y quien pidió ayuda humana. La etapa `ignore` **no notifica nunca**. Van agrupados por urgencia y la campana **se tiñe de lo más grave que haya**. El botón de «atender» solo aparece donde el aviso ES el interruptor: en molesto y listo se resuelve moviendo el pipeline. | `app/api/agent/handoff-alerts/route.ts`, `components/notification-bell.tsx` | Endpoint protegido (401 sin sesión). Build sin errores de tipos. | ⚠️ Desplegado, pendiente de prueba del dueño |
| 2026-08-01 | **La etapa del pipeline se ve, no se adivina.** Era un punto de **8 px** en la esquina derecha de cada fila: un cliente molesto y uno recién llegado se distinguían por eso. En una bandeja con decenas de chats, atender a alguien enfadado creyendo que es un contacto nuevo es un problema serio. Ahora la etapa se marca en tres sitios: el **círculo del contacto** se viste de su color con borde, una **franja** recorre la fila entera, y una **etiqueta escribe el nombre** (Sin atender, Negociando, Ya compró, Molesto, Ignorar). El nombre escrito es deliberado: el color solo se pierde en una lista larga y no sirve para quien no distingue rojo de verde, que son justo los dos que más importan aquí. | `components/chat/conversation-list.tsx` | Reportado por el dueño con captura. Build sin errores de tipos. | ⚠️ Desplegado, pendiente de prueba del dueño |
| 2026-08-01 | **Cuentas de pago: lista sin límite y titular abreviado.** Era un texto suelto pensado para bancos colombianos. Ahora cada cuenta tiene entidad, tipo, número y titular, y la entidad es texto libre: **PayPal, Binance, Wise** o lo que exista mañana. El titular se escribe **completo** y el sistema lo abrevia solo antes de que el bot lo diga (`Oscar Herrera Lopez` → `Oscxx Herxxxx Lopxx`), con vista previa en el panel. Se hizo así, y no pidiéndole al dueño que lo escriba tapado, para que no dependa de que se acuerde: un nombre completo junto a un número de cuenta es dato sensible y el chat queda en el teléfono del cliente. | `lib/agent/system-prompt.ts` (`enmascararTitular()`), `lib/personalization/actions.ts`, `app/(app)/personalizacion/client-form.tsx` | Build sin errores de tipos. **Sin probar end-to-end: el panel exige sesión iniciada.** | ⚠️ Desplegado, pendiente de prueba del dueño |
| 2026-08-01 | **Textos del panel en lenguaje llano.** El dueño entendió al revés el campo *Frase de reconducción*: creía que `{scope}` se reemplazaba por lo que pedía el **cliente**, y que a quien pidiera unicornios el bot le diría *«te puedo ayudar en unicornios»*. La ayuda decía solo *«Usa {scope} para insertar lo que vendes»*, que se presta a esa lectura. Ahora el campo se llama **«Qué contesta cuando le piden algo que usted no vende»** y la ayuda trae el ejemplo completo. Renombrados también: saludo → *Primera frase que dice el bot*; alcance → *Qué vende su negocio*; medios de pago → *Cómo puede pagar*; condiciones → *Cuándo se paga*. | `app/(app)/personalizacion/client-form.tsx` | Confusión reportada textualmente por el dueño. | ✅ Desplegado |
| 2026-08-01 | **En Precios de Marcación no se podía crear nada.** El dueño: *«no tengo ni idea de cómo editar estos servicios y mucho menos crear nuevos»*. Comprobado: `saveProduct` **siempre supo crear** (su `id` es opcional), pero ningún botón abría el editor en blanco — desde el panel solo se podían editar los que ya existían. Se añade **«+ Nuevo servicio»** y una explicación de cómo editar tarifas y qué significa el ámbar (sin tarifas, el bot no puede cotizarlo). | `app/(app)/conocimiento/KnowledgeBaseClient.tsx` | Lectura de `actions.ts`: no existía ningún `handleCreateProduct`. | ⚠️ Desplegado, pendiente de prueba del dueño |
| 2026-08-01 | **Los datos del negocio salen del panel, nunca del código.** `DEFAULT_PERSONA` traía `'Bancolombia, Nequi, Daviplata o PSE'` y `'50% para iniciar producción'`: si el campo del panel estaba vacío, el bot los soltaba como ciertos. Eran datos de UN negocio escritos en el código. Ahora esos valores por defecto están **vacíos** y, sin dato, el prompt **omite la frase entera**. Campos nuevos: **a dónde paga el cliente** (cuenta, titular, identificación — no existía en ninguna parte, y era lo que el bot rellenaba al preguntarle *"¿a dónde pago?"*), **datos que se piden al cerrar** (estaban fijos: nombre/razón social, NIT o cédula, correo, dirección), **página web**, **garantía**, **tiempos de entrega**, y una lista libre de **temas** (título + contenido) que es lo que permite servir a cualquier oficio sin tocar código. | `lib/agent/system-prompt.ts`, `lib/personalization/actions.ts`, `app/(app)/personalizacion/client-form.tsx` | Auditoría de datos duros en la plantilla del prompt. Build sin errores de tipos. **Sin probar end-to-end: el sandbox exige sesión iniciada y no se puede ejecutar por SSH.** | ⚠️ Desplegado, pendiente de prueba del dueño |
| 2026-08-01 | **La base de conocimiento vacía deja de ser un agujero.** `queryKnowledgeBase` consultaba `knowledge_chunks` — **0 filas**, scripts de ingesta nunca ejecutados — así que el bot recibía nada cada vez que le preguntaban por políticas: pregunta concreta + cero datos + una instrucción que afirma que ahí debería haberlos, el escenario exacto de la alucinación. Mientras tanto el panel **ya guardaba** dirección, teléfono, correo, política de cancelación y horarios, y el bot no podía leerlos. Ahora la herramienta lee de ahí (`fichasDelNegocio()`, `buscarEnFichas()`): determinista, sin embeddings, actualizado al guardar. Si no hay dato, devuelve **la lista de temas configurados** para que el bot reconduzca en vez de inventar. | `lib/agent/tools/query-knowledge-base.ts` | `content-range: */0` en `knowledge_chunks`; herramienta registrada en `lib/agent/index.ts:755` y ordenada en `system-prompt.ts:141`. | ✅ Desplegado |
| 2026-08-01 | **El candado de precios no bloquea las políticas.** Medido: solo dispara con cifras que llevan `$`, así que garantías, plazos y horarios pasaban sin tocarse. El único caso que bloqueaba era una política **con un monto** (*"envío gratis sobre $200.000"*), que el dueño había escrito él mismo y el bot no podía repetir. Ahora se aceptan las cifras devueltas por `queryKnowledgeBase` **en ese mismo turno**. Un precio de producto sigue exigiendo la calculadora. | `lib/agent/index.ts` | Lectura del guardrail: `responseText.match(/\$[\d.,]+/g)` y `PRICE_TOOLS`. | ✅ Desplegado |
| 2026-08-01 | **Una sola foto, la del producto del que se habla.** El cliente pidió la foto del cuaderno de 80 hojas y recibió tres. Causa: la selección descartaba las palabras de 4 letras o menos, o sea **los números**, que son lo único que distingue 80 de 100 y de 120 hojas; quedaban `cuaderno`, `argollado` y `hojas`, comunes a los tres, y bastaba UNA coincidencia. Ahora se puntúa y gana uno solo: la referencia exacta decide por sí sola, los números puntúan diez veces más que las palabras, y **si hay empate el bot pregunta** de cuál quiere la foto. | `lib/whatsapp/webhook-processor.ts` | Conversación real del dueño: 3 imágenes tras pedir una. | ⚠️ Desplegado, pendiente de prueba del dueño |
| 2026-08-01 | **La transcripción de audio ya funciona.** El documento la daba por rota por saldo de OpenRouter (402). Medido hoy: `Successfully transcribed audio, length: 47` con la nota de voz real *"Hola, quiero cotizar unas agenditas"*, y el bot cotizó cuadernos. | — (verificación) | Log de producción de `linea_1`. | ✅ Verificado |
| 2026-07-31 | **Un solo puente para todas las líneas.** El sistema trataba a cada línea distinto **por configuración**: `BRIDGE_LINES` y `FORWARD_INBOUND_LINES` enumeraban `linea_1,linea_2`, así que cualquier línea nueva quedaba fuera **en silencio**. Se retiran: vacías = todas. El puente descubre las líneas de `whatsapp_lines` más las sesiones en disco (`arrancarLineas()`, `lineasEnLaBase()`), y declara las registradas sin sesión como «pendiente de QR» en vez de omitirlas. La ranura es independiente del número: no hay ningún teléfono escrito en el código. | `wa-server-baileys/server.js`, `docker-compose.yml`, `.env.production` | Búsqueda de números hardcodeados en todo el repo: 0 en código. Arranque real: `Atendiendo 2 línea(s)`. Ambas líneas conectadas al mismo puente, misma configuración, `keepAliveErrors: 0`. | ✅ OK |
| 2026-07-31 | **El 463 no era del `@lid` ni de Baileys: era una cuenta bloqueada.** La causa documentada el 30-jul era falsa y sobre ella se habían construido dos puentes y un reparto de líneas. Medido enviando desde el servidor: línea A→línea B ✅, línea A→personal ✅, línea B→sí misma ✅, y **todo** lo que salía de la línea B hacia terceros ❌ 463, con `@lid` o con teléfono. Baileys sí envía: el mismo código entrega desde una línea y es rechazado desde otra, así que el bloqueo es de la **cuenta de WhatsApp** (temporal, por volumen automático — *timelocked*). No se anota qué número fue: es un estado pasajero y anotarlo solo genera malentendidos después. | — (diagnóstico) | Seis envíos reales desde el propio puente, tabla completa en `ESTADO_OPERATIVO.md`. | ✅ Diagnosticado |
| 2026-07-31 | **`whatsapp-web.js` queda descartado: su almacén está roto.** No era solo la media — fallaba también `getChats()`, con el mismo `[Error] r`. Se probó fijar la versión de WhatsApp Web: **no surte efecto** (pedida `2.3000.1040516757-alpha`, cargada `2.3000.1044236315`); la librería ya está en su última versión publicada (1.34.7). Contenedor detenido. | `wa-server/server.js` (autoprueba `/media-selftest`), `docker-compose.yml` | La autoprueba devuelve `getState: ok`, `info.wid: ok` y `getChats: FALLA` — la conexión está sana, el almacén no. | ✅ Descartado con prueba |
| 2026-07-31 | **Cada mensaje se respondía dos veces.** WhatsApp reemite el mismo mensaje: primero sin resolver el teléfono y de nuevo 750 ms después con `senderPn`. El puente reenviaba las dos copias, así que una nota de voz se transcribía y cotizaba dos veces. Ahora la segunda copia solo sirve para **aprender** el teléfono y se descarta en el puente, antes de gastar descarga, transcripción y modelo. | `wa-server-baileys/server.js` (`yaEntregado()`) | Registro del duplicado: `10:24:56.06 senderPn:null` y `10:24:56.81 senderPn:573015745403`, mismo audio de 10 KB. | ✅ Desplegado |
| 2026-07-31 | **El bot mandaba fotos que nadie pidió.** Al recibir una imagen, el sistema anota el texto con `[Imagen adjunta de cliente: ...]`, y la regla que decide enviar fotos buscaba la palabra «imagen» en el texto del cliente: **leía su propia anotación como una petición**. Se separa lo que el cliente dijo de verdad (`palabrasDelCliente`) de las anotaciones del sistema. Una foto del cliente ya no dispara fotos; pedirlas hablando, sí. | `lib/whatsapp/webhook-processor.ts` | Log del caso: 3 fotos enviadas tras una imagen del cliente, y consulta al catálogo contaminada con `"imagen adjunta organiza"`. | ✅ Desplegado |
| 2026-07-31 | **El panel mostraba las imágenes sin su información.** Dos fallos juntos: el panel buscaba la etiqueta `[Foto del cliente:` mientras el servidor escribía `[Imagen adjunta de cliente:` — dos nombres para lo mismo, nunca coincidían; y sin etiqueta descartaba el texto entero, así que las fotos de producto salían sin nombre, referencia ni precio aunque a WhatsApp sí llegaran completas. | `components/chat/message-bubble.tsx` | Comparación del texto guardado (`*Bolsa Organza 20x30 cm* (Ref: ZM-GEN-171) — $1.900 COP c/u`) contra lo que pintaba el panel. | ✅ Desplegado |
| 2026-07-31 | **El panel enseñaba `181290854776961@lid` como si fuera el teléfono.** El puente aprendía el teléfono real (`key.senderPn`) pero **se lo guardaba para sí**. Ahora viaja en el webhook (`senderPhone`) y se guarda en `contacts.metadata.telefono`; `wa_phone` no se toca, porque es la clave de enrutado del agente. En vez de arreglarlo pantalla por pantalla, **una sola función `mostrarContacto()`** decide cómo se ve un contacto en todo el panel. | `wa-server-baileys/server.js` (`telefonoRealDe()`), `lib/whatsapp/{config,adapter,contact-identity,webhook-processor}.ts`, `components/chat/conversation-list.tsx`, `app/(app)/conversaciones/[id]/client-page.tsx` | `telefonosAprendidos: 1` en ambas líneas; mapeo real observado: `181290854776961@lid → 573015745403`. Build sin errores de tipos. | ✅ Desplegado |
| 2026-07-30 | **WhatsApp rechazaba cada respuesta con `error 463`: se enviaba a un `@lid`.** Síntoma: el bot respondía en el panel y al cliente NO le llegaba nada. Nada fallaba por el camino — `text_sent: 2`, `send_errors: 0`, id válido devuelto — porque el rechazo llega DESPUÉS, en el acuse: `from: 181290854776961@lid class: message error: 463`. El chat estaba direccionado por `@lid`, un identificador interno que no es un teléfono. Baileys 6.7.24 sí trae el teléfono real en `key.senderPn`: ahora el puente **aprende** la correspondencia al recibir, la persiste en el volumen y traduce al enviar. Si no la conoce devuelve **503 con la causa** en vez de fingir el envío. | `wa-server-baileys/server.js` | Estado tras desplegar: `linea_2` conectada, 1 socket, 0 conflictos, `telefonosAprendidos` publicado en `/diagnostic`. | ⏳ Pendiente la prueba del dueño en WhatsApp real |
| 2026-07-30 | **Cinco defectos hallados tras la primera prueba real.** (a) *El primer mensaje de cada conversación nueva no se respondía*: la sincronización de historial traía el mensaje que se estaba procesando, lo insertaba, y la idempotencia hacía saltar el agente — el `'Hola'` de las 13:24:40 murió en silencio. (b) *Los mensajes que llegaban mientras el agente pensaba se descartaban*: la imagen de las 13:26:46 y el audio de las 13:27:03 se quedaron sin respuesta; ahora esperan turno hasta 45 s. (c) *Envíos fantasma*: el adaptador no miraba `res.ok`, inventaba un id y la app guardaba como enviada una respuesta que el cliente nunca recibió. (d) *La cita de un mensaje anterior no se veía*: el panel lee `data.message.quoted.body` y **ningún puente lo mandaba**; ahora Baileys sí, leyendo `contextInfo` de cualquier tipo de mensaje. (e) *Error #418 de React*: la hora se formateaba sin zona (servidor en UTC, navegador en local); fijada a `America/Bogota`. | `lib/whatsapp/webhook-processor.ts`, `lib/whatsapp/adapter.ts`, `wa-server-baileys/server.js`, `components/chat/message-bubble.tsx` | Los defectos (a) y (b) se confirmaron leyendo la conversación real del dueño en `linea_2`, no por hipótesis. Build sin errores de tipos; `linea_2` reconecta con 1 socket y 0 conflictos; `linea_1` intacta. | ✅ Desplegado |
| 2026-07-30 | **El puente Baileys estaba en un bucle y no atendía nada.** Tras vincular `linea_2`: `messages_received=8`, `media_detected=0`, `webhooks_sent=0`, y en el log **64 sockets abiertos con 114 `conflict: replaced`**. Causa: WhatsApp da UNA ranura por dispositivo y el manejador de cierre programaba una reconexión sin cerrar ni desmontar el socket anterior; los sockets se expulsaban entre sí. Corregido con un contador de generación `epoch` (los eventos de un socket sustituido se descartan), `teardownSocket()` antes de crear otro, un solo reintento en vuelo, y distinción de motivos de cierre (401 logout, 515 restart-required, 440 replaced, resto con retroceso exponencial). | `wa-server-baileys/server.js` | Medido después: **2 sockets, 0 conflictos**, `linea_2` estable 9 h con `keepAliveErrors: 0`. | ✅ OK |
| 2026-07-30 | **El eco del puente duplicaba cada respuesta.** El dueño vio cada mensaje dos veces. Medido: 15 de las últimas 40 salidas duplicadas, siempre el par `sent_*` + `openwa_*`. Causa: `send-text` del puente viejo devolvía `'sent_' + Date.now()`, un id inventado que jamás volvía a ver, así que al reenviar su propio mensaje la app no podía reconocerlo. Ahora devuelve el `_serialized` real, y además hay una compuerta en la app que no depende del id: una salida idéntica en la misma conversación en los últimos 90 s es el eco y se descarta. | `wa-server/server.js`, `lib/whatsapp/webhook-processor.ts` | En el bloque de Baileys cada respuesta aparece **una sola vez** (ids reales `3EB0…`); en el bloque anterior aparecía dos. | ✅ OK |
| 2026-07-30 | **La media ya llega al panel: el dueño ve las fotos y oye los audios.** `lib/r2-storage.ts` y `app/api/media/signed-url/route.ts` no existían en el VPS (se perdieron en una reversión). Repuestos y conectados: el webhook sube el archivo a Cloudflare R2 **en paralelo** con la transcripción/visión para no sumar latencia; en `messages.raw` queda la clave, el mimetype y el tamaño, **nunca el base64** (inflaría la base y volvería lento el panel); la burbuja pide una URL firmada de 1 h. Corregido además el mapeo de extensión: WhatsApp manda `audio/ogg; codecs=opus` y la tabla comparaba la cadena completa, así que toda nota de voz se guardaba como `.bin`. | `lib/r2-storage.ts`, `app/api/media/signed-url/route.ts`, `lib/whatsapp/webhook-processor.ts`, `components/chat/message-bubble.tsx` | `scripts/probar_media_e2e.py`: webhook simulado en `linea_2` → HTTP 200, clave `media/2026/07/….ogg` en la base, base64 fuera, tamaño correcto, y el objeto **verificado dentro del bucket `knowledgebot-fotos`** con su ContentType. Purgado al terminar. `/api/media/signed-url` responde 401 sin sesión. | ✅ OK |
| 2026-07-30 | **`linea_2` migrada al puente Baileys, con enrutado por línea.** Solo quedaban dos `.bak` del MVP de julio: era de una sola línea y solo leía imágenes. Se le cerraron **cinco huecos que habrían roto la línea en WhatsApp real sin avisar en el sandbox**: (1) multi-línea por `line_key`; (2) `send-media`, que no existía y habría dejado al bot sin enviar fotos de producto; (3) normalización del destinatario (la base guarda `@c.us` y Baileys exige `@s.whatsapp.net`); (4) `mediaError`/`mediaType` en el payload — sin ellos el adaptador descartaba el mensaje y **un audio fallido se perdía en silencio**; (5) paridad de contrato: `X-API-Key`, `sessions` en `/health` (el sync lo lee de ahí) y el QR en data URL porque el panel lo pinta con `<img src>`. | `wa-server-baileys/{server.js,Dockerfile,package.json}`, `lib/whatsapp/bridge.ts`, `docker-compose.yml`, `.env.production`, `wa-server/server.js`, 6 rutas de `app/api/` | Verificado: 3004 reporta solo `linea_1`; 3005 reporta `linea_2`; `[AUTOLOAD] 1 línea(s) para reconectar`. Descarga real medida: imagen 22 KB en 59 ms, audio 11 KB en 154 ms. | ✅ OK |
| 2026-07-30 | **Identidad del contacto: una persona, una ficha.** Fallo que YA estaba vivo sin Baileys: el contacto se buscaba por `wa_phone` exacto y cada puente nombra al mismo cliente distinto. `victor ramirez` tenía **tres** fichas y `VRS Digital` dos. Ahora se resuelve por dígitos con todas las variantes (`@c.us`, `@s.whatsapp.net`, `@lid`, sueltos), y la conversación pasa a ser por **(contacto, línea)**: al unificar las fichas, el mismo cliente escribiendo a dos líneas caería en un solo hilo. | `lib/whatsapp/contact-identity.ts` (nuevo), `lib/whatsapp/webhook-processor.ts`, `scripts/fusionar_contactos.py` | Fusión con respaldo previo en `backups/contactos_fusion_20260730_032456.json`: **13 → 11 contactos, 13 → 12 conversaciones, 882 → 882 mensajes** (cero pérdidas), 0 dígitos duplicados, 0 pares contacto+línea repetidos. | ✅ OK |
| 2026-07-29 | **Documentación unificada: los 9 `.md` dejan de contradecirse.** El dueño reportó que una IA se perdió leyendo documentos que decían cosas distintas. Causa medida: (a) `CLAUDE.md` existía **solo en el VPS** y `docs/planes/` **solo en local**, así que cada lado veía un proyecto distinto; (b) la memoria técnica afirmaba DeepSeek como modelo y "Meta Cloud API (Producción)", ambas falsas; (c) el README documentaba despliegue en Railway; (d) nadie había separado por escrito los **dos** sistemas de vectores (3072D del catálogo vs 1536D del glosario), la confusión más repetida. `AGENTS.md` pasa a ser el índice con el orden de autoridad y la lista de datos que los documentos viejos tienen mal; los obsoletos llevan aviso arriba y correcciones marcadas `CORRECCIÓN (2026-07-29)` en la línea exacta. | `AGENTS.md`, `README.md`, `knowledgebot_memoria_tecnica.md`, `docs/ESTADO_OPERATIVO.md`, `docs/planes/2026-07-18_AUDIO_LINEA_2.md`, `data/README_BASE_DE_DATOS.md`, `catalogo_catalogospromocionales/README.md`, `catalogo_catalogospromocionales/BASE_DE_DATOS/README_BASE_DE_DATOS.md` | Inventario de los `.md` en VPS y local; verificación de cada dato corregido contra el código y la base (`vector(1536)` en la migración, `outputDimensionality: 1536` en `lib/embeddings.ts`, `wa-server/` como directorio real del puente, compose sin Railway). Comparación de sumas SHA entre VPS y local al terminar. | ✅ Los 9 idénticos en ambos lados |
| 2026-07-29 | **La propuesta pasa a ser los 3 primeros del ranking, el más costoso de esos 3 primero.** Regla del dueño: *"que cotice los tres primeros de la lista que trae y de esos 3 que mire el más costoso y lo ofrezca de primer lugar. No es necesario ofrecer precios elevados; la opción premium que no se llame premium"*. `searchCatalog` marca `sugerido` 1/2/3 calculando el orden en el servidor con `max_price`, que se elimina del payload (el modelo sigue sin ver cifras). El prompt deja de exigir Premium/Estándar/Económica y de mandar buscar el más caro del catálogo. | `lib/agent/tools/search-catalog.ts`, `lib/agent/system-prompt.ts` | Sandbox: "50 mugs personalizados" → "Te tengo estas opciones" + ZM-MUG-004, ZM-MUG-003, ZM-MUG-007. | ✅ OK |
| 2026-07-29 | **La frase de fuera-de-alcance queda bloqueada si el catálogo sí tiene el producto**, aunque la respuesta cite productos. Observado en pruebas: a *"tienes algo en bambú o doble pared"* respondía *"eso puntual no lo manejamos"* y acto seguido ofrecía tres productos de bambú. Se compara contra la plantilla `offtopic_redirect` de Personalización, así que un "no manejamos esa medida exactamente" natural sigue permitido. | `lib/agent/index.ts` | Log: `GUARDRAIL: usó la frase de fuera-de-alcance teniéndolo en catálogo`, 5 coincidencias. | ✅ OK |
| 2026-07-29 | **Menos prompt, no más.** El dueño cuestionó con razón que se añadieran instrucciones. Medido: la plantilla del prompt pasó hoy de **314 a 199 líneas** (−115) y el código determinista creció +1.305 líneas. El bloque de catálogo que se inyecta por conversación se recortó de 6 líneas (3 de datos + 3 de instrucción) a **2 de datos puros**: la obligación la impone el código (búsqueda en servidor, entrega restringida a producción propia, guardrail que bloquea y reintenta), no una frase más. | `lib/agent/index.ts` | Tras quitar las instrucciones: organza 5x7 → $64.000, organza 20x30 → $95.000, 50 mugs → 3 mugs ZOOM. Nada se rompió. | ✅ OK |
| 2026-07-29 | **El comparador de palabras era asimétrico y hundía los productos propios.** El dueño reportó que el bot cotizaba mugs `MU-` importados y nunca llegaba a los `ZM-` (intentaba resolverlo con una cadena entre productos). Causa: `_contains_whole_word` añadía plurales al término pero no los quitaba, así que "mug" coincidía con "Set De **Mugs** Metálicos" pero "mugs" **no** coincidía con "**Mug** Tintero". Medido: `"mug"` → ZM-MUG-004/003/007 con 2,43; `"mugs"` → TE-347, MU-410, MU-162-1 con 1,10 y los ZM- del 4.º en adelante. | `Motor de Conocimiento/rag_query_engine.py` | Tras el arreglo, `"mugs"` devuelve los ZM- primeros con 2,70. Familias con ZOOM en cabeza: mugs, gorras, llaveros, cuadernos, camisetas, bolsas. Sin ZOOM: termos y bolígrafos, que no son producción propia. | ✅ OK |
| 2026-07-29 | **ZOOM primero, de forma determinista.** Con el motor ya devolviendo los propios en cabeza, la propuesta seguía saliendo con 2 importados y 1 ZOOM: la regla del embudo reserva la opción Premium para el más caro y un mug metálico importado cuesta 10 veces más que uno propio. Ahora, si hay 3 o más propios cotizables para esa búsqueda, `searchCatalog` **solo entrega propios** — el modelo no puede nombrar lo que no recibió, sin depender de ninguna instrucción del prompt. Con menos de 3, se completan con importados. El motor publica `es_propio` en `/query`. | `lib/agent/tools/search-catalog.ts`, `Motor de Conocimiento/api_service.py` | Sandbox: "50 mugs personalizados" → ZM-MUG-004, ZM-MUG-003, ZM-MUG-001 (3 de 3 ZOOM). "termos metálicos" → MU-355, MU-300, VA-570, porque ZOOM no los fabrica. | ✅ OK |
| 2026-07-29 | **El rail perdía el hilo de la conversación.** Prueba real: *"Quiero cotizar 100 bolsas de organza de 5x7"* se cotizó bien, pero al preguntar *"Y cuanto por 50 bolsas de las de 20por30 cm"* el bot cotizó una **Bolsa Satín 15x25**. El log lo mostró: la consulta fue `"bolsas"`, porque el segundo mensaje no repite "organza" y el rail solo miraba el mensaje actual. Ahora arrastra una palabra del historial del cliente (alfabética, nunca una medida vieja). | `lib/agent/index.ts` | Secuencia completa en sandbox: 5x7 → $64.000, 20x30 → $95.000, 14x20 → $61.000, todas con la referencia correcta. | ✅ En sandbox |
| 2026-07-29 | **Una palabra genérica envenena la búsqueda.** Medido contra el motor: `"bolsas organza"` deja la bolsa de 20x30 en la posición 9; `"organza 20x30"` la deja primera. El rail ordena ahora las palabras por cuántos productos activos las contienen ("organza" está en 7, "bolsas" en cientos) y consulta con las más específicas. Los conteos se cachean en memoria. | `lib/agent/index.ts` | Log `consulta usada`: `5x7 organza` → primero "Bolsa Organza 5x7 cm"; `20x30 bolsas organza` → primero "Bolsa Organza 20x30 cm". | ✅ OK |
| 2026-07-29 | **Medidas escritas a la colombiana.** `20por30`, `20 por 30` y `20 x 30` se normalizan a `20x30`, que es como están en el catálogo, y una medida que coincide pesa el cuádruple al ordenar la lista que ve el modelo. Las palabras del mensaje actual pesan x3 y las del historial solo x0,25, para que un cambio de tema no arrastre la lista. | `lib/agent/index.ts` | Regresión de cambio de tema: tras hablar de organza, "ahora dime que mugs tienes" encabeza la lista con un mug y responde con mugs. | ✅ OK |
| 2026-07-29 | **Rail de recuperación determinista.** Prueba real en WhatsApp: a "necesito bolsas de organza, unas 100 de 5x7" el bot respondió *"eso puntual no lo manejamos"*, inventó las referencias `BO-001`/`TX-001`/`IM-001` y ofreció maletines. El log lo probó: `fromTool: 0` — **searchCatalog nunca se ejecutó**, y al ser bloqueado buscó con las palabras de su propio texto de alcance ("bolso", "camiseta", "volantes") en vez de las del cliente. La regla vivía solo en el prompt. Ahora la búsqueda con las palabras del cliente corre SIEMPRE en el servidor antes de que el modelo hable, y negar un producto que sí está en el catálogo (sin ofrecer ninguna referencia) es un bloqueo del guardrail, `denied-with-catalog-hits`. | `lib/agent/index.ts`, `lib/agent/tools/search-catalog.ts` (nueva `runCatalogSearch`) | Sandbox con el mensaje literal que falló: responde **$64.000 el lote de 100 de 5x7** en 8,4 s. Sin regresión en saludos (el rail no se dispara sin palabras de contenido), mugs y volantes. | ✅ En sandbox; pendiente la prueba del dueño en WhatsApp |
| 2026-07-29 | **La frase entera envenenaba la búsqueda.** Al construir el rail se midió que el motor puntúa por palabras clave y el ruido la diluye: con `"Hola, necesito bolsas de organza, necesito unas 100 de 5por7"` devolvía **0 bolsas de organza**; con `"bolsas organza"` devuelve las 7. El rail consulta con los términos de contenido (sin cantidades ni medidas), reintenta con el término más específico y tolera singular/plural al cruzar. | `lib/agent/index.ts` | Medición directa contra `/query` antes y después. | ✅ OK |
| 2026-07-29 | **Preferencia ZOOM por producción propia, no por prefijo.** `is_zoom` era `reference.startswith('ZM-')`, así que los 6 volantes `VL-` y las 3 tarjetas `TJ-` —producción propia, de la hoja TARJETAS Y VOLANTES del Excel— no recibían ni un punto. Nuevo criterio: referencia `ZM-` **o** tener tarifa de una hoja del Excel propio (las hojas del CSV de importados quedan excluidas). Medido antes de aplicar: 500 por prefijo + 9 por hoja = 509, sin perder ninguno. | `Motor de Conocimiento/supabase_loader.py` | `/query "tarjetas de presentacion"` → TJ-UV, TJ-IM, TJ-BR encabezan con 2,97. `"volantes media carta"` → los 5 VL- primeros. | ✅ OK |
| 2026-07-29 | **14 bolsas de tela fabricadas, archivadas.** `ZM-GEN-407..420` (organza, satín y yute) no existen en ninguna hoja del Excel: medidas que el dueño confirmó inexistentes, sin foto, `unit='paquete'` y `source_sheet` vacío. Las cinco de yute eran copia exacta de `ZM-GEN-044/321/045/046/047` con el `price_basis` cambiado de `lote_total` a `unitario`; las de organza y satín tenían precios inventados en progresión aritmética. Se archivan (`active=false`, no se borran) y se purgan sus vectores. | Supabase `products`, índice `product_embeddings.json`, `scripts/reparar_bolsas_tela.py` | Respaldo en `backups/bolsas_20260729_*.json`. Catálogo 2.252 → 2.238 activos, cobertura vectorial 100%. Prueba en el sandbox: "¿tienen organza de 9x12?" → responde que no la maneja y ofrece la 10x12 real. | ✅ OK |
| 2026-07-29 | **Precio de lote cobrado como unitario: corregido en 5 bolsas legítimas.** `ZM-GEN-166/167/171/361/366` tenían sus tarifas como `unitario`, y `get-product-price.ts:367` multiplica por la cantidad: 100 bolsas de organza 5x7 se cotizaban a **$6.400.000** en vez de $64.000. Pasan a `lote_total` con rangos exactos 50‑50 y 100‑100, como sus hermanas de la hoja. `ZM-GEN-171` además corrige sus importes a los de la tabla del dueño ($95.000 / $180.000; la base decía $120.000 / $210.000). | Supabase `price_tiers`, `scripts/reparar_bolsas_tela.py` | Sandbox: "quiero 50 bolsas de organza 5x7" → **$21.000 el lote**, no $1.050.000. Yute 20x30 x100 → $430.000 por el producto correcto `ZM-GEN-047`. | ✅ OK |
| 2026-07-29 | **Subida de fotos desde el panel: reparada.** El endpoint y el botón ya existían (`c762a1f`, `006b311`, `eec4fcf`) pero **ningún upload podía funcionar**: el volumen de imágenes estaba montado `:ro`, así que `mkdir` fallaba y el endpoint devolvía 500 siempre. | `docker-compose.yml` | Prueba end-to-end sobre `VA-258 B`: HTTP 200 en 10,8 s, archivo en disco (8.470 B), `image_url` en Supabase, imagen servida por el panel y `image_count=1` en el índice vectorial. Estado revertido al terminar. | ✅ OK |
| 2026-07-29 | **El rollback del upload ya no borra la foto anterior.** Si el re-embed fallaba, el endpoint ponía `image_url = null` en vez de restaurar el valor previo: un fallo pasajero del motor borraba la foto de un producto que sí la tenía. Ahora se lee el `image_url` previo, se restaura al deshacer y se elimina la carpeta huérfana del disco. | `app/api/products/upload-image/route.ts` | Prueba del camino de fallo con referencia inexistente sobre `ZM-PAD-001`: HTTP 502, `image_url` intacto, 0 carpetas huérfanas. | ✅ OK |
| 2026-07-29 | **El re-embed ya no congela el motor de búsqueda ni pierde vectores.** `/reembed` tarda ~12 s y se ejecutaba en el event loop, dejando al bot sin `/query` mientras el panel subía una foto; además el read-modify-write del índice (138 MB) no estaba serializado y dos subidas simultáneas se pisaban. Ahora corre en `asyncio.to_thread` y la escritura del índice va bajo lock. | `Motor de Conocimiento/api_service.py`, `Motor de Conocimiento/embedding_pipeline.py` | Regresión de búsqueda tras el cambio: 3/3 consultas con resultados en 2,0–5,5 s y los `ZM-` encabezando. | ✅ OK |
| 2026-07-29 | **Deuda de bitácora saldada**: los commits `c762a1f` (botón Subir imagen), `006b311` (`/reembed` de un producto) y `eec4fcf` (endpoint `upload-image` + `sharp`) estaban en el VPS sin registrar aquí. | — | Lectura de `git log` del VPS. | ✅ Documentado |
| 2026-08-05 | **Barra de scroll espejo arriba en el Kanban (con medición al montar).** La barra nativa va abajo y queda enterrada bajo cientos de tarjetas. Barra espejo arriba (sticky): track oscuro + thumb índigo degradado, sincronizada bidireccional, **desplazamiento suave** (easeOutCubic 380ms) y **mide el tablero al montar** (con ) para que no nazca llena. No toca tarjetas, drag, scroll-snap ni pipeline; solo lee/escribe `scrollLeft`. | `app/(app)/kanban/client-page.tsx` | Rebuild + verificación del bundle. | ✅ OK (commit `cbc1c4b`) |
| 2026-08-05 | **Mini-mapa flotante de estados en el Kanban.** Con 7 columnas no se ven todas de un vistazo y la barra de scroll queda enterrada bajo cientos de tarjetas. Barra `glass` semitransparente centrada encima de las tarjetas, debajo de las pestañas de línea; al tocar un estado el tablero se desliza suave (`scrollTo` centrado) y un `onScroll` ilumina la columna actual. **Solo mueve la vista**: no toca tarjetas, stages, drag ni la lógica del pipeline. Solo desktop. | `app/(app)/kanban/client-page.tsx` | Rebuild + verificación del bundle compilado (`"Ir a"` presente en el chunk). | ✅ OK (commit `205428f`) |
| 2026-08-05 | **Barra de scroll lateral del Kanban visible.** Antes medía 6px y era casi invisible sobre fondo oscuro. Ahora 12px con gradiente índigo (coherente con el sistema), hover más claro y `overscroll-x` contenido. Solo CSS del bloque `<style>`; sin cambios en la lógica. | `app/(app)/kanban/client-page.tsx` | Rebuild + recarga fuerte. | ✅ OK (commit `d0d79a3`) |
| 2026-08-04 | **Blindaje de la campanita + robustez nginx anti-congelamiento HTTP/2.** El panel se congelaba de forma intermitente (`ERR_HTTP2_PROTOCOL_ERROR`). Causa raíz (Regla 0, con logs): 51 errores `499` en `access.log` + la campanita disparándose cada 30s desde cada pestaña abierta sobre el único cable HTTP/2 compartido. La app respondía en 4ms; **no era la app**. Fix: `AbortController` con timeout 8s (mata la petición colgada limpia), backoff 30s→60s→120s, pausa con Visibility API cuando la pestaña no se ve, no-disparar-dos-veces. nginx: `proxy_*_timeout 120s`, buffers 32k/64k, `proxy_next_upstream`. | `components/notification-bell.tsx`, `/etc/nginx/sites-enabled/knowledgebot` | 5 min de monitoreo del `access.log` post-deploy: sin nuevos `499`. | ✅ OK (commit `101fb29`) |
| 2026-07-29 | **Auditoría completa del sistema en producción.** Punto de restauración `92276ac`. | — | Lectura directa del VPS, la base y 833 mensajes reales. | ✅ Diagnóstico con evidencia. |
| 2026-07-29 | **Deduplicación del catálogo:** 1.280 referencias estaban repetidas hasta 9 veces. Se archivaron 6.301 filas conservando la de mejor calidad y migrando 395 tarifas. | Supabase `products`, `price_tiers` | Respaldo en `backups/products_20260729_*.json`. Conteo 8.637 → 2.252 activos. | ✅ 0 referencias duplicadas. |
| 2026-07-29 | **Enlace de imágenes:** 92 productos tenían su foto en disco sin vincular; se corrigieron además URLs rotas. | Supabase `products.image_url` | Verificación de existencia de cada archivo. | ✅ Cobertura 98,8%. |
| 2026-07-29 | **Identidad configurable desde el panel.** Causa raíz de que el bot se declarara IA: `agent_configs.system_prompt` decía "Eres un asistente virtual… escalas con un humano" y se anteponía al prompt que ordenaba lo contrario. | `lib/agent/system-prompt.ts`, `lib/personalization/actions.ts`, `app/(app)/personalizacion/client-form.tsx`, Supabase `agent_configs.metadata` | Prueba: "¿eres un robot?" → responde en personaje. | ✅ OK |
| 2026-07-29 | **Guardrails de salida**: referencias verificadas contra el catálogo, precios obligados a pasar por calculadora, bloqueo de propuestas sin precio y de interrogatorios sin oferta, saneamiento de identidad y formato de miles. | `lib/agent/index.ts` | Pruebas conversacionales end-to-end. | ✅ OK |
| 2026-07-29 | **Retrieval rail**: `searchCatalog` deja de exponer cifras al modelo (`price`, `max_price` y precios embebidos en descripciones). | `lib/agent/tools/search-catalog.ts` | Latencia por mensaje de 84s a 13s; desaparecen los reintentos por precios de memoria. | ✅ OK |
| 2026-07-29 | **Idempotencia de mensajes**: `ignoreDuplicates` no informaba del conflicto y el agente se ejecutaba dos veces (31% de respuestas duplicadas). Se añade verificación de inserción y cerrojo por conversación. | `lib/whatsapp/webhook-processor.ts` | Revisión de 521 mensajes salientes históricos. | ✅ OK |
| 2026-07-29 | **Envío de fotos bajo demanda**: se guardan las imágenes de las propuestas y se envían solo si el cliente las pide. | `lib/whatsapp/webhook-processor.ts` | Prueba: "¿tienes foto del segundo?" → confirma el envío sin negarse. | ✅ OK |
| 2026-07-29 | **Preferitismo ZOOM reparado.** `is_preferente` devolvía 0 de 2.252: leía `products.price` (vacío en Supabase) y no reconocía las referencias `ZM-`. | `Motor de Conocimiento/supabase_loader.py`, `rag_query_engine.py` | 517 preferentes; los ZM- encabezan camisetas, llaveros, cuadernos y DTF. | ✅ OK |
| 2026-07-29 | **DTF cotizable**: `calculateCustomPrice` buscaba por UUID pero recibía referencias, y el candado no la aceptaba como calculadora válida. | `lib/agent/tools/calculate-custom-price.ts`, `lib/agent/index.ts` | Cotización por medidas operativa. | ✅ OK |
| 2026-07-29 | **Motor RAG bajo systemd** (`knowledgebot-rag`) con reinicio automático y `reload=False`; se elimina la llamada extra al LLM por búsqueda que gastaba tokens y contradecía al catálogo. | `/etc/systemd/system/knowledgebot-rag.service`, `api_service.py`, `rag_query_engine.py` | `systemctl is-active` + consultas de prueba. | ✅ OK |
| 2026-07-29 | **Centro de Control con datos reales.** Antes era `FILTER_DATA` inventado. Ahora reporta servicios, líneas, catálogo, identidad y actividad, con causa y acción de reparación. | `app/api/health/route.ts` (nuevo), `app/(app)/control-room/page.tsx` | Respuesta verificada contra la base. | ✅ OK |
| 2026-07-29 | **Pestaña Servicios y Marcaciones con tarifas reales.** Antes mostraba precios escritos a mano en el código que no existían ni en la base ni en el Excel. | `app/(app)/conocimiento/actions.ts`, `KnowledgeBaseClient.tsx` | 85 servicios agrupados; señala los que no tienen tarifa. | ✅ OK |
| 2026-07-29 | **Reindexado de embeddings** (3072D multimodales) sobre el catálogo deduplicado, purgando huérfanos. | `Motor de Conocimiento/data/embeddings/` | Incremental con checkpoint. | ✅ OK |
| 2026-07-29 | **Documentación corregida**: `ESTADO_OPERATIVO.md` describía un puente Baileys en el puerto 3005 que no existe en producción. | `docs/ESTADO_OPERATIVO.md` | Verificación de contenedores y puertos activos. | ✅ OK |
| 2026-07-24 | **PUNTO DE RESTAURACIÓN RESTORE_POINT_20260724_PRE_ZM_PREFERENTE**: Restauración de puntaje preferente para referencias `ZM-` de ZOOM y activación de extracción de fotos/citas. | `Motor de Conocimiento/rag_query_engine.py`, `docs/REGISTRO_DE_CAMBIOS.md` | Commit git `376092a` / `7e0c100`. | ✅ OK |
| 2026-07-18 | Documentación operativa inicial. | `docs/ESTADO_OPERATIVO.md`, `docs/REGISTRO_DE_CAMBIOS.md`, `AGENTS.md` | Lectura cruzada de memoria vs código. | ✅ OK |
| 2026-07-21 | Restauración del pipeline multimodal (visión de imágenes + audio). | `lib/whatsapp/webhook-processor.ts` | Backup + tsc. | ⚠️ Revertido por el usuario. |
| 2026-07-21 | Reversión completa + limpieza de scripts `.py` con credenciales. | `webhook-processor.ts`, 20 scripts `.py` eliminados | grep de credenciales. | ✅ OK |
| 2026-07-21 | Mejora del panel de líneas: `isZombie` ahora evalúa `keepAliveErrors >= 10`. | `app/api/whatsapp-lines/diagnostic/route.ts`, `app/(app)/integraciones/whatsapp/client-page.tsx` | Prueba visual. | ✅ OK |
| 2026-07-21 | `protocolTimeout: 180000` en puente viejo + `Buffer.from` en describe-image. | `wa-server/server.js`, `lib/whatsapp/describe-image.ts` | Rebuild puente. | ⚠️ No solucionó el bug de descarga. |
| 2026-07-22 | **P0: Snapshot de producción en git (VPS).** Red de seguridad. | `.gitignore` (sesiones, `.bak`, logs), 68 archivos. | Commit `36d2213`, sin push. | ✅ OK |
| 2026-07-22 | **F2: MVP de Baileys en puerto 3005 (modo sombra).** | `wa-server-baileys/package.json`, `Dockerfile`, `server.js`, `docker-compose.yml` | Baileys descargó imagen de prueba: 91 KB en 238ms. | ✅ Hipótesis probada. |
| 2026-07-22 | **S1a: Enrutado de puente por línea** (`getBridgeUrl(lineKey?)` con `WHATSAPP_BRIDGE_ROUTES`). | `lib/whatsapp/bridge.ts` | Build app, mapa vacío = idéntico. | ✅ OK (commit 2942c0a) |
| 2026-07-22 | **S1b: Reconexión R2 + AWS SDK + package-lock.json.** | `package.json`, `package-lock.json` | Prueba aislada de R2: subida + lectura. | ✅ OK (commit 75f8bf8) |
| 2026-07-22 | **S1c: R2 + purga base64 + firma en lectura + unificación strings.** | `webhook-processor.ts`, `message-bubble.tsx`, nueva ruta `/api/media/signed-url` | Build app, `linea_2` intacta. | ✅ OK (commit 27827fb) |
| 2026-07-23 | **S2-PRE + S2: Conmutación de `linea_1` a Baileys.** Endpoints `/diagnostic`, `/logout`, `/send-text` funcional. `FORWARDING_ENABLED=true`. Ruta `linea_1→3005`. Fix `message`/`text` en send-text. Fix `syncHistory:false` para evitar timeout de init. Soporte audio/ptt. | `wa-server-baileys/server.js`, `docker-compose.yml`, `.env.production` | **Prueba end-to-end: foto → R2 → Gemini → Oscar responde.** | ✅ OK |
| 2026-07-23 | **Sincronización de Catálogo RAG → Supabase:** Migración de productos faltantes con paginación (`.range()`) y preservación de UUIDs. | `scripts/import_rag_to_supabase.ts`, Supabase DB (`products`, `price_tiers`) | Conteo en Supabase pasó de 2.317 a 8.617 productos. | ✅ OK |
| 2026-07-24 | **4 Ajustes Finos de Calidad Comercial (Software Defensivo):** Enmascaramiento de UUIDs a `REF-XXXXXXXX`, sanitizador Regex en `index.ts`, priorización ZOOM con `has_pricing: true`, eliminación de evasivas y respuesta entusiasta de imágenes. | `lib/agent/tools/search-catalog.ts`, `lib/agent/tools/get-product-price.ts`, `lib/agent/index.ts` | Verificación de tipos + sanitización determinista. | ✅ OK |
| 2026-07-24 | **Poblado de referencias comerciales ZM- en Supabase (517 productos) y all_products.json (488 productos) + Migración 00007 (Trigger antierror).** Backup creado: `all_products.json.bak`. | `scripts/populate_zoom_references.ts`, `supabase/migrations/00007_auto_generate_product_reference.sql`, `lib/agent/tools/search-catalog.ts`, `all_products.json` | Ejecución de script de lote + sanitización de rag_response. | ✅ 8.639 productos en Supabase con referencias comerciales completas. |
| 2026-07-24 | **Actualización de `lib/embeddings.ts` para soporte multiclave (GEMINI_API_KEY / GOOGLE_API_KEY / OPENROUTER_API_KEY) + fallback resiliente en Glosario.** | `lib/embeddings.ts` | Guardado de jerga comercial en el panel web. | ✅ Guardado de glosario 100% funcional. |
| 2026-07-24 | **Blindaje total en `saveGlosarioItem` (try/catch para evitar alertas) + adición de `posillo`/`posillos` en `JARGON_SYNONYMS` de Python RAG.** | `app/(app)/conocimiento/actions.ts`, `Motor de Conocimiento/rag_query_engine.py` | Pruebas de guardado de jerga + búsquedas de posillo en RAG. | ✅ OK |
| 2026-07-24 | **Soporte híbrido Gemini 2 + OpenRouter en `lib/embeddings.ts` (detección por prefijo de clave `AIza...` / `sk-or-v1...`).** | `lib/embeddings.ts` | Guardado de jerga comercial sin depender exclusivamente de GEMINI_API_KEY. | ✅ OK |

---

## Notas

- Las `linea_*` conectadas en cada momento son **líneas de prueba** y cambian a voluntad del dueño. El sistema en producción final tendrá **8 líneas** conectadas al CRM.
- Si un cambio requiere deploy, anotar fecha/hora del deploy y resultado de la verificación post-deploy.
