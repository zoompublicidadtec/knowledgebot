import * as fs from 'fs';
import Papa from 'papaparse';

const filePath = 'd:\\AUTOMATIZACIONES WHATSAPP\\base_datos_zoom_publicidad.csv';
const content = fs.readFileSync(filePath, 'utf-8');
const parsed = Papa.parse(content, { header: true, skipEmptyLines: true });
const textToProcess = parsed.data.map(row => JSON.stringify(row)).join('\n');
console.log('Total characters:', textToProcess.length);
console.log('Sample:', textToProcess.substring(0, 200));
