-- Seed data for KnowledgeBot SaaS
-- Reference seed file. Auth users and organization data are created from the app.

-- No direct seed inserts needed for auth users as auth is managed, but we can provide reference SQL if needed.
SELECT 1;
